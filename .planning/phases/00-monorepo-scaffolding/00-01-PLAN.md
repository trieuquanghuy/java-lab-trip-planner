---
phase: 00-monorepo-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - settings.gradle.kts
  - build.gradle.kts
  - gradle.properties
  - gradle/libs.versions.toml
  - gradle/wrapper/gradle-wrapper.properties
  - gradlew
  - gradlew.bat
  - gradle/wrapper/gradle-wrapper.jar
  - .gitignore
  - .editorconfig
  - README.md
autonomous: true
requirements: [NFR-04]

must_haves:
  truths:
    - "Spring Cloud train is 2025.0.x (Northfields) — NOT 2024.0.x. CLAUDE.md is incorrect on this point per D-30."
    - "`micrometer-tracing-bom` is pinned exactly once in `gradle/libs.versions.toml` and never overridden per-service (Pitfall 7 / D-26)."
    - "`gradle/libs.versions.toml` is the SINGLE source of truth for every dep version; no version literals outside the catalog."
    - "Gradle 8.14.x wrapper committed; not Gradle 9.x (Spring Boot 3.5 toolchain officially targets 8.x)."
    - "`.env.example` is committed; `.env` is gitignored. AUTH_JWT_SECRET ships as `dev-only-32-byte-secret-replace-in-prod` placeholder."
  artifacts:
    - path: "settings.gradle.kts"
      provides: "Multi-module structure declaring :services:* and :libs:* subprojects"
      contains: ":libs:observability, :libs:error-handling, :libs:api-contracts, :services:eureka-server, :services:api-gateway, :services:auth-service, :services:trip-service, :services:destination-service"
    - path: "build.gradle.kts"
      provides: "Root build with subprojects {} block (Java 21 toolchain, JUnit Platform, Jacoco)"
      contains: "JavaLanguageVersion.of(21)"
    - path: "gradle/libs.versions.toml"
      provides: "Single source of truth for every dependency version pin (Spring Boot 3.5.x, Spring Cloud 2025.0.x, Flyway, jjwt 0.13.0, Micrometer Tracing BOM, etc.)"
      contains: "springCloud = \"2025.0"
    - path: "gradle/wrapper/gradle-wrapper.properties"
      provides: "Gradle 8.14.x wrapper pin"
      contains: "gradle-8.14"
    - path: ".gitignore"
      provides: "Ignore Gradle build artifacts, frontend node_modules/dist, .env, IDE files"
      contains: ".env"
    - path: ".env.example"
      provides: "Committed env template per D-24 covering all variables in docs/08-deployment.md §1.4"
      contains: "AUTH_JWT_SECRET"
  key_links:
    - from: "every service/build.gradle.kts (Wave 2+)"
      to: "gradle/libs.versions.toml"
      via: "Gradle version-catalog type-safe accessors (libs.spring.boot.starter.web)"
      pattern: "libs\\.spring|libs\\.flyway|libs\\.bundles\\.observability"
    - from: ".env.example"
      to: "infra/docker-compose.yml (Wave 4)"
      via: "Compose env_file directive / environment substitution"
      pattern: "AUTH_DB_USER|AUTH_DB_PASSWORD|POSTGRES_DB"
---

<objective>
Lay down the build infrastructure foundation: Gradle Kotlin DSL multi-module skeleton, single-source-of-truth version catalog, wrapper, root build conventions, environment template, and root README. Every later wave depends on this — no service `build.gradle.kts` can resolve dependencies without `gradle/libs.versions.toml` first existing.

Purpose: Lock the Spring Cloud 2025.0.x correction (D-30) and the `micrometer-tracing-bom` single-pin rule (Pitfall 7) into the catalog from the very first commit so no later plan can drift.

Output: A repository with a working Gradle multi-module skeleton (no source files yet — just build config), a committed wrapper, a comprehensive `.env.example`, and a quickstart README documenting the include-alias compose layout and 30–60s warm-up window.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-monorepo-scaffolding/00-CONTEXT.md
@.planning/phases/00-monorepo-scaffolding/00-RESEARCH.md
@.planning/phases/00-monorepo-scaffolding/00-PATTERNS.md
@CLAUDE.md
@docs/02-architecture.md
@docs/08-deployment.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1.1: Create Gradle multi-module skeleton (settings, root build, gradle.properties, wrapper)</name>
  <files>settings.gradle.kts, build.gradle.kts, gradle.properties, gradle/wrapper/gradle-wrapper.properties, gradlew, gradlew.bat, gradle/wrapper/gradle-wrapper.jar</files>
  <read_first>
    - .planning/phases/00-monorepo-scaffolding/00-CONTEXT.md (lines 149-153 — D-26, D-27, D-28, D-29 — Gradle conventions)
    - .planning/phases/00-monorepo-scaffolding/00-PATTERNS.md (lines 138-211 — Bucket A code excerpts for settings.gradle.kts, build.gradle.kts, gradle.properties)
    - .planning/phases/00-monorepo-scaffolding/00-RESEARCH.md (lines 300-385 — Recommended Project Structure)
    - docs/02-architecture.md §5 — repo layout
  </read_first>
  <action>
    1. Generate Gradle 8.14.x wrapper. From repo root run `gradle wrapper --gradle-version 8.14.2 --distribution-type bin` (or use a temporary `gradle` install via SDKMAN/brew). If `gradle` is not available locally, manually populate `gradle/wrapper/gradle-wrapper.properties` with `distributionUrl=https\://services.gradle.org/distributions/gradle-8.14.2-bin.zip` and download the matching `gradle-wrapper.jar` (sha256 verifiable from `https://services.gradle.org/distributions/`); copy `gradlew` + `gradlew.bat` from the Gradle 8.14.2 distribution. Per D-27 the wrapper MUST be 8.14.x (not 9.x).
    2. Write `settings.gradle.kts` verbatim from PATTERNS.md Bucket A "settings.gradle.kts" (rootProject.name = "trip-planner"; pluginManagement repositories = gradlePluginPortal + mavenCentral; dependencyResolutionManagement repositories = mavenCentral; include `:libs:observability`, `:libs:error-handling`, `:libs:api-contracts`, `:services:eureka-server`, `:services:api-gateway`, `:services:auth-service`, `:services:trip-service`, `:services:destination-service`). Note: subprojects are declared but their build files are added in later waves; Gradle will warn until those `build.gradle.kts` files exist — that is expected for Wave 1.
    3. Write `build.gradle.kts` (root) verbatim from PATTERNS.md Bucket A "build.gradle.kts (root)" — declares `plugins { java }`, `allprojects { group = "com.tripplanner"; version = "0.1.0-SNAPSHOT" }`, and `subprojects { apply(plugin = "java"); apply(plugin = "jacoco"); JavaLanguageVersion.of(21); mavenCentral; useJUnitPlatform() }`. Per D-29 this enforces the Java 21 toolchain consistently across every subproject without each having to repeat itself.
    4. Write `gradle.properties` verbatim from PATTERNS.md Bucket A: `org.gradle.parallel=true`, `org.gradle.caching=true`, `org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8`.
    5. Make `gradlew` executable: `chmod +x gradlew`.

    Do NOT add any subproject `build.gradle.kts` files in this task — those are owned by waves 2/3. The skeleton must permit `./gradlew tasks` to run successfully (or surface a helpful "subproject does not exist yet" message that does not block) — verify with the acceptance criteria below.
  </action>
  <verify>
    <automated>test -f gradlew && test -f gradlew.bat && test -f gradle/wrapper/gradle-wrapper.jar && test -f gradle/wrapper/gradle-wrapper.properties && grep -q 'gradle-8.14' gradle/wrapper/gradle-wrapper.properties && grep -q 'rootProject.name = "trip-planner"' settings.gradle.kts && grep -q ':libs:observability' settings.gradle.kts && grep -q ':services:api-gateway' settings.gradle.kts && grep -q 'JavaLanguageVersion.of(21)' build.gradle.kts && grep -q 'org.gradle.parallel=true' gradle.properties && [ -x gradlew ]</automated>
  </verify>
  <acceptance_criteria>
    - `gradle/wrapper/gradle-wrapper.properties` contains `gradle-8.14` (some 8.14.x patch).
    - `settings.gradle.kts` declares all 8 subproject includes (3 libs + 5 services).
    - `build.gradle.kts` configures `JavaLanguageVersion.of(21)` and applies `jacoco` to all subprojects.
    - `gradlew` is executable (file mode contains `x`).
    - `./gradlew --version` outputs Gradle version 8.14.x and JVM 21 (run after Java 21 is available; in CI this is `actions/setup-java@v4 java-version: 21`).
  </acceptance_criteria>
  <done>Gradle wrapper resolved; root build config locks Java 21; settings include all 8 subprojects (build files arrive in later waves).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 1.2: Author the version catalog (gradle/libs.versions.toml) — single source of truth</name>
  <files>gradle/libs.versions.toml</files>
  <read_first>
    - .planning/phases/00-monorepo-scaffolding/00-CONTEXT.md (lines 149-152 — D-26 catalog rule, D-30 Spring Cloud 2025.0.x)
    - .planning/phases/00-monorepo-scaffolding/00-RESEARCH.md (lines 393-427 — Pattern 1 catalog excerpt; lines 142-191 — every backend version pin; lines 964-975 — Pitfall E Spring Cloud version drift)
    - .planning/phases/00-monorepo-scaffolding/00-PATTERNS.md (line 31, line 54-55 — Bucket A reference; Convention C6, C16, C17)
    - CLAUDE.md `## Technology Stack` table — backend version pins (Spring Boot 3.5.x, Java 21, jjwt 0.13.0, Resilience4j 2.4.0, etc.). NOTE: D-30 OVERRIDES CLAUDE.md's Spring Cloud pin — use 2025.0.x, NOT 2024.0.x.
  </read_first>
  <action>
    Create `gradle/libs.versions.toml` with three sections (`[versions]`, `[libraries]`, `[bundles]`, `[plugins]`) per Gradle version-catalog convention. Use the excerpt at RESEARCH.md lines 393-427 as the starting template and EXTEND it with every dep referenced anywhere in the phase plans. Specifically:

    `[versions]` block (each value is a literal pin — no ranges):
    - `java = "21"`
    - `springBoot = "3.5.14"` (latest 3.5.x as of 2026-04; Spring Boot 3.3.x is OSS-EOL — do NOT use it)
    - `springCloud = "2025.0.2"` — **MANDATORY: 2025.0.x (Northfields) per D-30, NOT 2024.0.x**. Add an inline TOML comment: `# D-30: Spring Cloud 2025.0 (Northfields) matches Spring Boot 3.5.x. CLAUDE.md cites 2024.0.x — that pin is wrong; 2024.0 (Moorgate) is for SB 3.4.x.`
    - `postgresqlJdbc = "42.7.4"`
    - `flyway = "10.21.0"` (note: SB 3.5 manages Flyway 10.x — do not override unless necessary)
    - `micrometerTracing = "1.4.5"` — **pinned ONCE here per Pitfall 7 / D-26 / C6. Do NOT override per-service.**
    - `logstashLogbackEncoder = "7.4"`
    - `jjwt = "0.13.0"` (catalogued for Phase 1; Phase 0 does not use it)
    - `resilience4j = "2.4.0"` (catalogued for Phase 4)
    - `wiremockSpringBoot = "3.9.0"` (catalogued for Phase 4)
    - `junit = "5.12.0"`, `mockito = "5.x"` (let SB BOM manage; pin only if needed)
    - `testcontainers = "1.21.0"`

    `[libraries]` block — declare module coordinates (no version refs where the BOM manages it; explicit version.ref where necessary):
    - All Spring Boot starters: `spring-boot-starter-web`, `spring-boot-starter-webflux`, `spring-boot-starter-actuator`, `spring-boot-starter-data-jpa`, `spring-boot-starter-test`, `spring-boot-testcontainers`
    - Spring Cloud: `spring-cloud-starter-gateway`, `spring-cloud-starter-netflix-eureka-server`, `spring-cloud-starter-netflix-eureka-client`
    - Persistence: `flyway-core`, `flyway-database-postgresql` (— **MANDATORY explicit dep per Pitfall A; comment in the catalog: `# Pitfall A: Flyway 10 modularized PG driver. MUST be runtimeOnly on every DB-backed service.`**), `postgresql-jdbc`
    - Observability: `micrometer-tracing-bridge-otel`, `opentelemetry-exporter-zipkin`, `logstash-logback-encoder` (with `version.ref = "logstashLogbackEncoder"`)
    - JWT (catalogued for Phase 1): `jjwt-api`, `jjwt-impl`, `jjwt-jackson` (all version.ref `"jjwt"`)
    - Test/QA: `wiremock-spring-boot` (version.ref `"wiremockSpringBoot"`)

    `[bundles]` block:
    - `observability = ["micrometer-tracing-bridge-otel", "opentelemetry-exporter-zipkin", "logstash-logback-encoder"]`

    `[plugins]` block:
    - `spring-boot = { id = "org.springframework.boot", version.ref = "springBoot" }`
    - `spring-dependency-management = { id = "io.spring.dependency-management", version = "1.1.6" }`

    Top-of-file header comment: `# SINGLE SOURCE OF TRUTH for every dep version (D-26 / Convention C16). No version literals outside this file. micrometer-tracing-bom pinned ONCE here per Pitfall 7 / Convention C6.`

    Per Convention C16, no service `build.gradle.kts` may declare a version literal — every coordinate must reference this catalog via `libs.<accessor>` or `mavenBom("...:${libs.versions.<key>.get()}")`.
  </action>
  <verify>
    <automated>grep -q 'springCloud = "2025\.0' gradle/libs.versions.toml && ! grep -E 'springCloud = "2024\.0' gradle/libs.versions.toml && grep -q 'flyway-database-postgresql' gradle/libs.versions.toml && grep -q 'micrometer-tracing-bridge-otel' gradle/libs.versions.toml && grep -q 'opentelemetry-exporter-zipkin' gradle/libs.versions.toml && grep -q 'logstash-logback-encoder' gradle/libs.versions.toml && grep -q 'spring-cloud-starter-gateway' gradle/libs.versions.toml && grep -q 'spring-cloud-starter-netflix-eureka-server' gradle/libs.versions.toml && grep -q 'spring-cloud-starter-netflix-eureka-client' gradle/libs.versions.toml && grep -qE 'springBoot = "3\.5\.' gradle/libs.versions.toml && grep -qE 'jjwt = "0\.13\.' gradle/libs.versions.toml && grep -q '\[bundles\]' gradle/libs.versions.toml && grep -q 'observability = \[' gradle/libs.versions.toml</automated>
  </verify>
  <acceptance_criteria>
    - `springCloud = "2025.0.x"` literal present (some patch); 2024.0.x NOT present anywhere.
    - `flyway-database-postgresql` library entry present (Pitfall A enforcement starts here).
    - `micrometer-tracing-bridge-otel` + `opentelemetry-exporter-zipkin` + `logstash-logback-encoder` all in `[libraries]` and `[bundles].observability`.
    - Spring Cloud Gateway + Eureka Server + Eureka Client starters all present.
    - Spring Boot version is 3.5.x (some patch).
    - jjwt 0.13.0 catalogued (for Phase 1 consumer).
    - File contains an explanatory header comment about D-26 single-source-of-truth.
  </acceptance_criteria>
  <done>Catalog locks every backend version pin; D-30 Spring Cloud correction applied; Pitfall 7 single `micrometer-tracing-bom` pin enforced; Pitfall A `flyway-database-postgresql` available for all DB-backed services to depend on.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 1.3: Create .gitignore, .editorconfig, .env.example, root README.md</name>
  <files>.gitignore, .editorconfig, .env.example, README.md</files>
  <read_first>
    - .planning/phases/00-monorepo-scaffolding/00-CONTEXT.md (lines 145-148 — D-24 env-only secrets; "Specifics" lines 281-289 — `.env.example` "DO NOT COMMIT" header, root README compose alias note)
    - .planning/phases/00-monorepo-scaffolding/00-PATTERNS.md (lines 213-275 — Bucket A `.gitignore`, `.env.example`, root README excerpts)
    - docs/08-deployment.md §1.3 — port assignments table; §1.4 — env var list (canonical source for `.env.example`)
    - .planning/phases/00-monorepo-scaffolding/00-RESEARCH.md (lines 947-962 — Pitfall D Eureka warm-up window, document in README)
    - Existing `.gitignore` (read current contents to extend, not overwrite)
  </read_first>
  <action>
    1. Update `.gitignore` (extend existing). Append the block from PATTERNS.md Bucket A `.gitignore` excerpt covering: `.gradle/`, `build/`, `frontend/node_modules/`, `frontend/dist/`, `frontend/.vite/`, `.env`, `.env.local`, `.idea/`, `*.iml`, `.vscode/`, `.DS_Store`. Do NOT remove any existing entries.

    2. Create `.editorconfig` with cross-editor convention:
       ```
       root = true

       [*]
       charset = utf-8
       end_of_line = lf
       insert_final_newline = true
       trim_trailing_whitespace = true
       indent_style = space

       [*.{java,kt,kts}]
       indent_size = 4

       [*.{ts,tsx,js,jsx,json,yml,yaml,html,css,md}]
       indent_size = 2

       [Makefile]
       indent_style = tab
       ```

    3. Create `.env.example` per D-24 + docs/08-deployment.md §1.4. Open with the comment header: `# DO NOT COMMIT a .env file with real values. This is the dev template only.\n# Source: docs/08-deployment.md §1.4 — every var listed there must appear here.`. Then list every variable from PATTERNS.md Bucket A `.env.example` excerpt verbatim:
       - Postgres: `POSTGRES_DB=tripplanner`, `POSTGRES_USER=postgres`, `POSTGRES_PASSWORD=postgres`, `POSTGRES_HOST=localhost`, `POSTGRES_PORT=5432`
       - Per-service DB users (D-08): `AUTH_DB_USER=auth_svc`, `AUTH_DB_PASSWORD=auth_svc`, `TRIP_DB_USER=trip_svc`, `TRIP_DB_PASSWORD=trip_svc`, `DESTINATION_DB_USER=destination_svc`, `DESTINATION_DB_PASSWORD=destination_svc`
       - Eureka: `EUREKA_URL=http://localhost:8761/eureka`
       - Tracing: `ZIPKIN_BASE_URL=http://localhost:9411`
       - JWT (D-24 placeholder for Phase 1): `AUTH_JWT_SECRET=dev-only-32-byte-secret-replace-in-prod`
       - Frontend: `VITE_API_URL=http://localhost:8080`

       After writing, cross-check against `docs/08-deployment.md §1.4`. If that section lists any variable not present here (e.g. `MAIL_HOST`, `MAIL_PORT`, `REDIS_HOST`, `REDIS_PORT`), add it as well using a sensible local-dev default. The intent: `.env.example` is the union of every env variable any service or compose entry will read.

    4. Create `README.md` at repo root with the following sections (markdown content, no code excerpts elided):
       - **Title:** "Trip Planner"
       - **Tagline:** one-sentence pitch from `.planning/PROJECT.md` ("Plan multi-day trips, discover attractions, and assemble itineraries.")
       - **Quickstart**:
         1. Prerequisites: Docker Desktop 4.30+ (for Compose `include:`), Java 21 (Temurin), Node 20 LTS, pnpm 9 (auto-bootstrapped via Corepack).
         2. `cp .env.example .env` (do NOT edit unless overriding defaults).
         3. `docker compose up -d --wait` (wraps to all-healthy in <60s; the root `docker-compose.yml` is an `include:` alias for `infra/docker-compose.yml` per D-19).
         4. Open: `http://localhost:5173` (frontend), `http://localhost:8080/actuator/health` (gateway), `http://localhost:8761` (Eureka dashboard), `http://localhost:8025` (Mailhog UI), `http://localhost:9411` (Zipkin UI).
         5. Run smoke: `bash scripts/smoke.sh` — exits 0 if all 5 ROADMAP success criteria pass.
       - **Architecture summary**: 1 paragraph + bulleted port table from `docs/08-deployment.md §1.3` (gateway 8080, auth 8081, trip 8082, destination 8083, eureka 8761, frontend 5173, postgres 5432, redis 6379, mailhog 1025/8025, zipkin 9411). Note the loopback-only binding per D-22 for downstream services + postgres + eureka.
       - **Cold-start window note** (per Pitfall D / 00-RESEARCH.md lines 947-962): "Eureka registration adds a 30–60s warm-up window on first `docker compose up`. The compose stack uses healthchecks + `depends_on: condition: service_healthy` (D-20) and tuned Eureka intervals (D-21) to keep this under 60s."
       - **Build & test**: `./gradlew check` (backend), `pnpm --filter frontend test --run && pnpm --filter frontend build` (frontend).
       - **Repository layout**: bulleted tree mirroring 00-RESEARCH.md "Recommended Project Structure" (services/, libs/, frontend/, infra/, .github/workflows/, scripts/).
       - **Stack pins note** (one paragraph): "Backend uses Spring Boot 3.5.x + Spring Cloud 2025.0.x (Northfields). The CLAUDE.md tech-stack table cites Spring Cloud 2024.0.x — that pin is incorrect; 2024.0 (Moorgate) is the Spring Boot 3.4 train. We use 2025.0 per D-30. Frontend pins enforce Axios ≥1.15.0 (CVE-fixed), Tailwind v3.4.x, React 18.3.x, react-leaflet pinned at v4.2.x for Phase 8."
       - **Documentation links**: `docs/02-architecture.md`, `docs/08-deployment.md`, `.planning/ROADMAP.md`.
       - **License & status**: portfolio/practice project; not for production.
  </action>
  <verify>
    <automated>grep -q '^\.env$' .gitignore && grep -q 'frontend/node_modules' .gitignore && grep -q 'AUTH_JWT_SECRET=dev-only' .env.example && grep -q 'AUTH_DB_USER=auth_svc' .env.example && grep -q 'TRIP_DB_USER=trip_svc' .env.example && grep -q 'DESTINATION_DB_USER=destination_svc' .env.example && grep -q 'VITE_API_URL=http://localhost:8080' .env.example && grep -q 'DO NOT COMMIT' .env.example && grep -q 'docker compose up' README.md && grep -q '8761' README.md && grep -q '5173' README.md && grep -q -i 'spring cloud' README.md && grep -q '2025.0' README.md && grep -q 'indent_size' .editorconfig</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` includes `.env` (so a developer who runs `cp .env.example .env` and edits secrets does not accidentally commit them).
    - `.env.example` contains all 6 per-service DB user/password env pairs (3 services × 2 = 6 vars), the JWT placeholder, `VITE_API_URL`, and a "DO NOT COMMIT" header comment.
    - `.editorconfig` enforces LF line endings + 2-space indent for TS/YAML/JSON/MD and 4-space indent for Java/Kotlin.
    - `README.md` documents `docker compose up -d --wait`, the 30–60s warm-up window note, the include-alias compose layout (D-19), the port table, and the Spring Cloud 2025.0.x correction.
  </acceptance_criteria>
  <done>Repo-root infrastructure committed: developers can clone, `cp .env.example .env`, and proceed; the README accurately documents the stack including the D-30 correction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine ↔ git/GitHub | Source code + secrets boundary; misclassified files (`.env`) leak credentials into history |
| build classpath ↔ Maven Central / npm registry | Untrusted upstream artifacts pulled into the build (supply-chain) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-00-01 | Information Disclosure | `.env` committed to git with real secrets | HIGH | mitigate | `.gitignore` lists `.env` + `.env.local`; `.env.example` ships placeholders only with a "DO NOT COMMIT" header comment (Task 1.3 acceptance). |
| T-00-02 | Tampering / Supply-Chain | Maven Central / npm dep substitution attack via floating versions | MEDIUM | mitigate | Every backend version is a literal pin in `gradle/libs.versions.toml` (Convention C16); no version ranges. Frontend pins land in plan 12. |
| T-00-03 | Tampering | Gradle wrapper jar tampered with malicious build steps | MEDIUM | mitigate | Use the official Gradle 8.14.2 wrapper jar (sha256-verifiable from services.gradle.org). Wrapper jar committed once and not re-generated unless explicitly upgrading. |
| T-00-04 | Information Disclosure | README leaks security-sensitive port info to external readers | LOW | accept | README documents only loopback-bound dev ports; production deployment is out of scope (v1 = local only). |
| T-00-05 | Repudiation | Spring Cloud version drift between CLAUDE.md (2024.0.x) and verified train (2025.0.x) silently breaks dependency resolution at downstream wave | HIGH | mitigate | Version catalog header comment + README "Stack pins note" both call out the D-30 correction explicitly so reviewers don't propagate the CLAUDE.md error. |
</threat_model>

<verification>
- `./gradlew --version` (after Java 21 in PATH) reports Gradle 8.14.x and JVM 21.
- `cat gradle/libs.versions.toml | grep -c 'springCloud = "2025.0"'` returns `1`.
- `cat gradle/libs.versions.toml | grep -c 'springCloud = "2024.0"'` returns `0` (no incorrect pin).
- `git check-ignore .env` (after `touch .env`) returns `.env` (proof it is ignored).
</verification>

<success_criteria>
- Gradle 8.14.x wrapper present, executable, sha-aligned with services.gradle.org distribution.
- `gradle/libs.versions.toml` is the single source of truth: Spring Boot 3.5.x, Spring Cloud 2025.0.x (D-30), Flyway core + `flyway-database-postgresql`, Micrometer Tracing BOM pinned once, observability bundle declared, jjwt 0.13.0 catalogued for Phase 1.
- `.gitignore` ignores `.env`, `.gradle/`, `build/`, `frontend/node_modules/`.
- `.env.example` lists every variable required by every later wave (postgres, per-service DB users, eureka URL, zipkin URL, JWT placeholder, frontend API URL).
- `README.md` is the single-page entry that documents quickstart, port table, warm-up window, and the D-30 stack correction.
</success_criteria>

<output>
After completion, create `.planning/phases/00-monorepo-scaffolding/00-01-SUMMARY.md` summarizing:
- Wrapper version installed.
- Spring Cloud version pinned in catalog (must be 2025.0.x).
- Total libraries declared in `[libraries]` (count).
- Files created and their byte sizes (smoke check that nothing is empty).
</output>
