---
phase: 06-trip-service-itinerary-items-favorites
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gradle/libs.versions.toml
  - services/trip-service/build.gradle.kts
  - services/trip-service/src/main/resources/db/migration/V5__add_photo_url_to_items.sql
  - services/trip-service/src/main/resources/db/migration/V6__create_favorites.sql
  - libs/error-handling/src/main/java/com/tripplanner/errors/ErrorCode.java
autonomous: true
requirements:
  - TRIP-03
  - PERS-02
  - PERS-03
  - NFR-06
must_haves:
  truths:
    - "itinerary_items table has a photo_url column"
    - "favorites table exists with composite PK (user_id, destination_ref)"
    - "Jsoup dependency available in trip-service"
    - "Error codes TRIP_DAY_NOT_IN_TRIP and TRIP_ITEM_NOT_FOUND exist"
  artifacts:
    - path: "services/trip-service/src/main/resources/db/migration/V5__add_photo_url_to_items.sql"
      provides: "photo_url column on itinerary_items"
    - path: "services/trip-service/src/main/resources/db/migration/V6__create_favorites.sql"
      provides: "favorites table with composite PK and indexes"
    - path: "gradle/libs.versions.toml"
      provides: "Jsoup version entry"
    - path: "libs/error-handling/src/main/java/com/tripplanner/errors/ErrorCode.java"
      provides: "TRIP_DAY_NOT_IN_TRIP and TRIP_ITEM_NOT_FOUND error codes"
  key_links:
    - from: "services/trip-service/build.gradle.kts"
      to: "gradle/libs.versions.toml"
      via: "libs.jsoup dependency accessor"
      pattern: "implementation.*jsoup"
---

<objective>
Add Flyway migrations for photo_url column and favorites table, Jsoup dependency for XSS sanitization, and new error codes for item/favorite operations.

Purpose: Foundation for item CRUD and favorites — schema changes and dependencies must exist before service logic.
Output: V5 + V6 migrations, Jsoup in version catalog + build.gradle.kts, 2 new ErrorCode entries.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-trip-service-itinerary-items-favorites/06-CONTEXT.md
@.planning/ROADMAP.md
@docs/03-data-model.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flyway V5 + V6 migrations and Jsoup dependency</name>
  <files>
    gradle/libs.versions.toml
    services/trip-service/build.gradle.kts
    services/trip-service/src/main/resources/db/migration/V5__add_photo_url_to_items.sql
    services/trip-service/src/main/resources/db/migration/V6__create_favorites.sql
  </files>
  <read_first>
    - gradle/libs.versions.toml (current versions and libraries sections)
    - services/trip-service/build.gradle.kts (current dependencies)
    - services/trip-service/src/main/resources/db/migration/V4__create_itinerary_items.sql (existing schema)
    - docs/03-data-model.md §3.5-3.6 (itinerary_items photo_url, favorites table spec)
  </read_first>
  <action>
1. Add Jsoup to gradle/libs.versions.toml:
   - In [versions] section: `jsoup = "1.18.1"`
   - In [libraries] section: `jsoup = { module = "org.jsoup:jsoup", version.ref = "jsoup" }`

2. Add Jsoup to services/trip-service/build.gradle.kts:
   - Add `implementation(libs.jsoup)` after the validation starter line

3. Create V5__add_photo_url_to_items.sql:
   ```sql
   -- Phase 6 D-11: denormalized photo_url for cover image fallback.
   ALTER TABLE trip.itinerary_items ADD COLUMN photo_url VARCHAR(2048) NULL;
   ```

4. Create V6__create_favorites.sql per docs/03-data-model.md §3.6:
   ```sql
   -- Phase 6 D-06/D-08: favorites table with composite PK.
   CREATE TABLE trip.favorites (
       user_id          UUID         NOT NULL,
       destination_ref  VARCHAR(80)  NOT NULL,
       created_at       TIMESTAMPTZ  NOT NULL DEFAULT NOW(),
       PRIMARY KEY (user_id, destination_ref)
   );

   CREATE INDEX favorites_user_created_idx ON trip.favorites (user_id, created_at DESC);
   ```
  </action>
  <acceptance_criteria>
    - gradle/libs.versions.toml contains `jsoup = "1.18.1"` in [versions]
    - gradle/libs.versions.toml contains `jsoup = { module = "org.jsoup:jsoup"` in [libraries]
    - services/trip-service/build.gradle.kts contains `implementation(libs.jsoup)`
    - V5__add_photo_url_to_items.sql contains `ALTER TABLE trip.itinerary_items ADD COLUMN photo_url VARCHAR(2048)`
    - V6__create_favorites.sql contains `CREATE TABLE trip.favorites` with `PRIMARY KEY (user_id, destination_ref)`
    - V6__create_favorites.sql contains `CREATE INDEX favorites_user_created_idx`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/huyqtrieu/Desktop/Practice/java-lab && ./gradlew :services:trip-service:dependencies --configuration compileClasspath --no-daemon -q 2>&1 | grep -i jsoup</automated>
  </verify>
  <done>Jsoup on classpath, V5 and V6 migrations ready, build resolves cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Extend ErrorCode enum with item/favorite codes</name>
  <files>libs/error-handling/src/main/java/com/tripplanner/errors/ErrorCode.java</files>
  <read_first>
    - libs/error-handling/src/main/java/com/tripplanner/errors/ErrorCode.java (current enum entries)
  </read_first>
  <action>
Add two new entries to the ErrorCode enum after TRIP_CONCURRENT_MODIFICATION:
- `TRIP_DAY_NOT_IN_TRIP("trip.day_not_in_trip")` — 400, target dayId doesn't belong to this trip
- `TRIP_ITEM_NOT_FOUND("trip.item_not_found")` — 404, item doesn't exist or user doesn't own it
  </action>
  <acceptance_criteria>
    - ErrorCode.java contains `TRIP_DAY_NOT_IN_TRIP("trip.day_not_in_trip")`
    - ErrorCode.java contains `TRIP_ITEM_NOT_FOUND("trip.item_not_found")`
    - File compiles without errors
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/huyqtrieu/Desktop/Practice/java-lab && ./gradlew :libs:error-handling:compileJava --no-daemon -q 2>&1; echo "EXIT:$?"</automated>
  </verify>
  <done>ErrorCode enum has TRIP_DAY_NOT_IN_TRIP and TRIP_ITEM_NOT_FOUND entries, compiles cleanly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted input (note text, destinationRef, photoUrl) crosses here |
| trip-service→PostgreSQL | SQL injection boundary (parameterized queries via JPA) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing | favorites table | mitigate | user_id from JWT, not request body — enforced at service layer |
| T-06-02 | Tampering | itinerary_items.note | mitigate | Jsoup Safelist.none() strips all HTML before persist (D-04/D-05) |
| T-06-03 | Info Disclosure | item ownership | mitigate | 404 not 403 for cross-user access — same as Phase 5 D-09 |
| T-06-04 | Tampering | itinerary_items.photo_url | accept | Display-only URL, no server-side fetch — CSP on frontend prevents script injection via URL |
</threat_model>

<verification>
- `./gradlew :services:trip-service:compileJava --no-daemon -q` exits 0
- `./gradlew :libs:error-handling:compileJava --no-daemon -q` exits 0
- grep confirms Jsoup on classpath
</verification>

<success_criteria>
V5 and V6 migrations exist, Jsoup dependency resolves, ErrorCode enum extended with 2 new codes, entire project compiles.
</success_criteria>

<output>
After completion, create `.planning/phases/06-trip-service-itinerary-items-favorites/06-01-SUMMARY.md`
</output>
