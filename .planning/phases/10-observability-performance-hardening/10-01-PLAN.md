---
phase: 10-observability-performance-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/api-gateway/src/main/resources/application.yml
  - services/auth-service/src/main/resources/application.yml
  - services/trip-service/src/main/resources/application.yml
  - services/destination-service/src/main/resources/application.yml
  - libs/observability/src/main/java/com/tripplanner/observability/ObservabilityAutoConfiguration.java
  - infra/docker-compose.yml
  - scripts/trace-verify.sh
autonomous: true
requirements: [NFR-09]
must_haves:
  truths:
    - "A create-trip→add-item→reorder flow produces a single Zipkin trace spanning gateway + trip-service + DB"
    - "traceId is identical in all service JSON logs for a single request"
    - "Structured JSON logs include traceId, spanId, userId, requestId"
  artifacts:
    - path: "scripts/trace-verify.sh"
      provides: "Automated trace continuity verification script"
    - path: "libs/observability/src/main/java/com/tripplanner/observability/ObservabilityAutoConfiguration.java"
      provides: "MDC enrichment with userId and requestId"
  key_links:
    - from: "api-gateway"
      to: "trip-service"
      via: "W3C traceparent header propagation"
      pattern: "traceparent"
---

<objective>
Verify and harden distributed tracing across all services.

Purpose: Confirm NFR-09 — end-to-end W3C trace context propagation with structured JSON logs containing traceId, spanId, userId, requestId.
Output: Verified trace continuity, trace-verify.sh script for regression.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@libs/observability/src/main/java/com/tripplanner/observability/ObservabilityAutoConfiguration.java
@libs/observability/src/main/java/com/tripplanner/observability/MdcEnrichmentFilter.java
@infra/docker-compose.yml
@infra/prometheus/prometheus.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and fix trace propagation configuration</name>
  <files>
    services/api-gateway/src/main/resources/application.yml
    services/auth-service/src/main/resources/application.yml
    services/trip-service/src/main/resources/application.yml
    services/destination-service/src/main/resources/application.yml
    libs/observability/src/main/java/com/tripplanner/observability/ObservabilityAutoConfiguration.java
  </files>
  <action>
    Verify each service has these configurations enabled:
    1. `management.tracing.sampling.probability=1.0` (dev profile)
    2. `management.zipkin.tracing.endpoint` points to Zipkin (already in application-docker.yml)
    3. The MdcEnrichmentFilter populates MDC keys: traceId, spanId, userId (from X-User-Id header), requestId (from X-Request-Id header)
    4. Logback JSON encoder includes MDC fields in output

    If any MDC field (userId, requestId) is missing from the filter, add it. Ensure the ObservabilityAutoConfiguration registers the filter with proper ordering (before business logic).

    Verify Spring Cloud Gateway propagates trace headers downstream (spring.cloud.gateway.filter.request-header-to-context should include traceparent).
  </action>
  <verify>
    <automated>cd /Users/huyqtrieu/Desktop/Practice/java-lab && grep -r "traceId\|spanId\|userId\|requestId" libs/observability/src/main/java/ | wc -l</automated>
  </verify>
  <done>All 4 MDC keys (traceId, spanId, userId, requestId) are enriched in the observability filter; all services have sampling=1.0 in dev</done>
</task>

<task type="auto">
  <name>Task 2: Create trace verification script</name>
  <files>scripts/trace-verify.sh</files>
  <action>
    Create scripts/trace-verify.sh that:
    1. Starts docker compose (if not running) using infra/docker-compose.yml
    2. Waits for services to be healthy (poll actuator/health)
    3. Mints a test JWT using scripts/mint-test-token.sh
    4. Performs: POST /api/trips (create trip) → POST /api/trips/{id}/days/{dayId}/items (add item) → PATCH /api/trips/{id}/items/{itemId} (reorder)
    5. Extracts traceId from response headers (traceparent) or Zipkin API
    6. Queries Zipkin API (GET /api/v2/trace/{traceId}) and verifies:
       - Trace contains spans from api-gateway AND trip-service
       - All spans share the same traceId
    7. Exits 0 on success, 1 on failure with diagnostic output

    Make the script executable. Use curl for HTTP calls.
    The script should be idempotent and runnable standalone.
  </action>
  <verify>
    <automated>test -x scripts/trace-verify.sh && head -5 scripts/trace-verify.sh | grep -q "#!/bin/bash"</automated>
  </verify>
  <done>trace-verify.sh exists, is executable, performs end-to-end trace verification against Zipkin API</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Observability endpoints | Actuator/prometheus and Zipkin ports exposed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | I (Information Disclosure) | /actuator/prometheus | mitigate | Expose only health,info,prometheus — no env or configprops |
| T-10-02 | I (Information Disclosure) | Zipkin UI | accept | Local-only; docker-compose binds to 127.0.0.1 in prod |
</threat_model>

<verification>
- trace-verify.sh passes when run against docker compose environment
- All service logs contain traceId, spanId in structured JSON format
</verification>

<success_criteria>
End-to-end trace visible in Zipkin spanning api-gateway → trip-service with consistent traceId across all service logs.
</success_criteria>

<output>
After completion, create `.planning/phases/10-observability-performance-hardening/10-01-SUMMARY.md`
</output>
