---
phase: 5
plan: 2
title: "JPA entities + repositories + exception classes + DTOs"
type: execute
wave: 1
depends_on: []
requirements: [TRIP-01, TRIP-02, TRIP-06]
files_modified:
  - services/trip-service/src/main/java/com/tripplanner/trip/domain/Trip.java
  - services/trip-service/src/main/java/com/tripplanner/trip/domain/ItineraryDay.java
  - services/trip-service/src/main/java/com/tripplanner/trip/domain/ItineraryItem.java
  - services/trip-service/src/main/java/com/tripplanner/trip/repository/TripRepository.java
  - services/trip-service/src/main/java/com/tripplanner/trip/repository/ItineraryDayRepository.java
  - services/trip-service/src/main/java/com/tripplanner/trip/repository/ItineraryItemRepository.java
  - services/trip-service/src/main/java/com/tripplanner/trip/service/exception/TripNotFoundException.java
  - services/trip-service/src/main/java/com/tripplanner/trip/service/exception/ShortenConflictException.java
  - services/trip-service/src/main/java/com/tripplanner/trip/service/exception/InvalidDateRangeException.java
  - services/trip-service/src/main/java/com/tripplanner/trip/api/dto/CreateTripRequest.java
  - services/trip-service/src/main/java/com/tripplanner/trip/api/dto/UpdateTripRequest.java
  - services/trip-service/src/main/java/com/tripplanner/trip/api/dto/TripResponse.java
  - services/trip-service/src/main/java/com/tripplanner/trip/api/dto/TripListResponse.java
  - services/trip-service/src/main/java/com/tripplanner/trip/api/dto/DayResponse.java
autonomous: true
must_haves:
  truths:
    - "Trip entity maps to trip.trips table with all columns"
    - "ItineraryDay entity maps to trip.itinerary_days table with all columns"
    - "ItineraryItem entity maps to trip.itinerary_items table (for COUNT query only)"
    - "Repositories support ownership-filtered queries (findByIdAndUserId)"
    - "DTOs enforce validation constraints (@NotBlank, @Size)"
    - "ShortenConflictException carries orphaned day info for 409 response body"
  artifacts:
    - path: "services/trip-service/src/main/java/com/tripplanner/trip/domain/Trip.java"
      provides: "Trip JPA entity"
      contains: "@Table(name = \"trips\", schema = \"trip\")"
    - path: "services/trip-service/src/main/java/com/tripplanner/trip/repository/TripRepository.java"
      provides: "Ownership-filtered trip queries"
      contains: "findByIdAndUserId"
    - path: "services/trip-service/src/main/java/com/tripplanner/trip/api/dto/CreateTripRequest.java"
      provides: "Trip creation DTO with validation"
      contains: "@NotBlank"
  key_links:
    - from: "TripRepository.java"
      to: "Trip.java"
      via: "JpaRepository<Trip, UUID>"
      pattern: "JpaRepository<Trip, UUID>"
    - from: "ItineraryItemRepository.java"
      to: "ItineraryItem.java"
      via: "countByDayIds JPQL for shrink-conflict check"
      pattern: "countByDayIds"
---

<objective>
Create all JPA entities, Spring Data repositories, custom exception classes, and request/response DTOs for the trip service. This establishes the complete type system that the service layer (Plan 03) and controller layer (Plan 04) consume.

Purpose: Define contracts — entities map to DB schema, repositories provide data access, DTOs define the API shape, exceptions define error semantics.
Output: 14 Java files across domain, repository, exception, and DTO packages.
</objective>

<context>
@.planning/phases/05-trip-service-trips-days/05-CONTEXT.md (D-02, D-03, D-06, D-09, D-13)
@.planning/phases/05-trip-service-trips-days/05-RESEARCH.md (§1 patterns, §4 DTOs)
@.planning/phases/05-trip-service-trips-days/05-PATTERNS.md (entity, repo, exception, DTO patterns)
@docs/03-data-model.md (§3.3–3.5 column types)
@services/auth-service/src/main/java/com/tripplanner/auth/domain/User.java (entity analog)
@services/auth-service/src/main/java/com/tripplanner/auth/repository/UserRepository.java (repo analog)
@services/auth-service/src/main/java/com/tripplanner/auth/service/exception/InvalidCredentialsException.java (exception analog)
@services/auth-service/src/main/java/com/tripplanner/auth/api/dto/SignupRequest.java (DTO analog)
@libs/api-contracts/src/main/java/com/tripplanner/contracts/UserContext.java (userId is String)

<interfaces>
<!-- From libs/api-contracts — executor needs this contract -->
From libs/api-contracts/src/main/java/com/tripplanner/contracts/UserContext.java:
```java
public record UserContext(String userId, String email, boolean verified) implements Principal {
    @Override public String getName() { return userId; }
}
```

From libs/error-handling/src/main/java/com/tripplanner/errors/ErrorCode.java (after Plan 01):
```java
TRIP_NOT_FOUND("trip.not_found"),
TRIP_SHORTEN_CONFLICT("trip.shorten_requires_confirmation"),
TRIP_INVALID_DATES("trip.invalid_date_range");
```
</interfaces>
</context>

<tasks>

### Task 1: Create JPA entities (Trip, ItineraryDay, ItineraryItem)

<task>
<read_first>
- services/auth-service/src/main/java/com/tripplanner/auth/domain/User.java (exact entity pattern — annotations, constructor, getters/setters, updatedAt pattern)
- docs/03-data-model.md (§3.3 trips columns, §3.4 itinerary_days columns, §3.5 itinerary_items columns)
- services/trip-service/src/main/resources/application.yml (confirm schema = "trip", ddl-auto = validate)
</read_first>

<action>
**A) Create `services/trip-service/src/main/java/com/tripplanner/trip/domain/Trip.java`:**
```java
package com.tripplanner.trip.domain;

import jakarta.persistence.Column;
import jakarta.persistence.Entity;
import jakarta.persistence.Id;
import jakarta.persistence.Table;

import java.time.Instant;
import java.time.LocalDate;
import java.util.UUID;

@Entity
@Table(name = "trips", schema = "trip")
public class Trip {

    @Id
    @Column(name = "id", nullable = false, updatable = false)
    private UUID id;

    @Column(name = "user_id", nullable = false, updatable = false)
    private UUID userId;

    @Column(name = "name", nullable = false, length = 120)
    private String name;

    @Column(name = "start_date")
    private LocalDate startDate;

    @Column(name = "end_date")
    private LocalDate endDate;

    @Column(name = "cover_image_url", length = 2048)
    private String coverImageUrl;

    @Column(name = "created_at", nullable = false, updatable = false)
    private Instant createdAt;

    @Column(name = "updated_at", nullable = false)
    private Instant updatedAt;

    protected Trip() {}   // JPA

    public Trip(UUID id, UUID userId, String name, LocalDate startDate, LocalDate endDate) {
        this.id = id;
        this.userId = userId;
        this.name = name;
        this.startDate = startDate;
        this.endDate = endDate;
        Instant now = Instant.now();
        this.createdAt = now;
        this.updatedAt = now;
    }

    public UUID getId() { return id; }
    public UUID getUserId() { return userId; }
    public String getName() { return name; }
    public LocalDate getStartDate() { return startDate; }
    public LocalDate getEndDate() { return endDate; }
    public String getCoverImageUrl() { return coverImageUrl; }
    public Instant getCreatedAt() { return createdAt; }
    public Instant getUpdatedAt() { return updatedAt; }

    public void setName(String name) { this.name = name; this.updatedAt = Instant.now(); }
    public void setStartDate(LocalDate d) { this.startDate = d; this.updatedAt = Instant.now(); }
    public void setEndDate(LocalDate d) { this.endDate = d; this.updatedAt = Instant.now(); }
    public void setCoverImageUrl(String u) { this.coverImageUrl = u; this.updatedAt = Instant.now(); }
}
```

Key conventions (matching User.java analog):
- `protected Trip() {}` for JPA, public constructor for application code
- `UUID id` field with `@Id`, no `@GeneratedValue` (app generates UUID)
- `updatable = false` on id, userId, createdAt
- Setters update `this.updatedAt = Instant.now()` (same pattern as User.java)
- No `@ManyToOne` relationships — FK stored as UUID column

**B) Create `services/trip-service/src/main/java/com/tripplanner/trip/domain/ItineraryDay.java`:**
```java
package com.tripplanner.trip.domain;

import jakarta.persistence.Column;
import jakarta.persistence.Entity;
import jakarta.persistence.Id;
import jakarta.persistence.Table;

import java.time.LocalDate;
import java.util.UUID;

@Entity
@Table(name = "itinerary_days", schema = "trip")
public class ItineraryDay {

    @Id
    @Column(name = "id", nullable = false, updatable = false)
    private UUID id;

    @Column(name = "trip_id", nullable = false, updatable = false)
    private UUID tripId;

    @Column(name = "day_date", nullable = false)
    private LocalDate dayDate;

    @Column(name = "day_index", nullable = false)
    private int dayIndex;

    protected ItineraryDay() {}

    public ItineraryDay(UUID id, UUID tripId, LocalDate dayDate, int dayIndex) {
        this.id = id;
        this.tripId = tripId;
        this.dayDate = dayDate;
        this.dayIndex = dayIndex;
    }

    public UUID getId() { return id; }
    public UUID getTripId() { return tripId; }
    public LocalDate getDayDate() { return dayDate; }
    public int getDayIndex() { return dayIndex; }
}
```

No updatedAt, no setters — days are immutable once created (deleted and re-created during materialization).

**C) Create `services/trip-service/src/main/java/com/tripplanner/trip/domain/ItineraryItem.java`:**
```java
package com.tripplanner.trip.domain;

import jakarta.persistence.Column;
import jakarta.persistence.Entity;
import jakarta.persistence.Id;
import jakarta.persistence.Table;

import java.time.Instant;
import java.time.LocalTime;
import java.util.UUID;

@Entity
@Table(name = "itinerary_items", schema = "trip")
public class ItineraryItem {

    @Id
    @Column(name = "id", nullable = false, updatable = false)
    private UUID id;

    @Column(name = "itinerary_day_id", nullable = false)
    private UUID itineraryDayId;

    @Column(name = "destination_ref", nullable = false, length = 80)
    private String destinationRef;

    @Column(name = "position", nullable = false)
    private int position;

    @Column(name = "time_slot")
    private LocalTime timeSlot;

    @Column(name = "note", length = 500)
    private String note;

    @Column(name = "created_at", nullable = false, updatable = false)
    private Instant createdAt;

    @Column(name = "updated_at", nullable = false)
    private Instant updatedAt;

    protected ItineraryItem() {}

    // No public constructor needed in Phase 5 — entity exists only for JPQL COUNT query
    public UUID getId() { return id; }
    public UUID getItineraryDayId() { return itineraryDayId; }
}
```

Minimal entity — only needs `@Entity` mapping so `ItineraryItemRepository` JPQL queries compile. Full CRUD is Phase 6.
</action>

<acceptance_criteria>
- File `services/trip-service/src/main/java/com/tripplanner/trip/domain/Trip.java` exists
- Trip.java contains `@Table(name = "trips", schema = "trip")`
- Trip.java contains `private UUID userId` with `updatable = false`
- Trip.java contains `public void setName(String name)` with `this.updatedAt = Instant.now()`
- Trip.java contains `protected Trip() {}`
- File `services/trip-service/src/main/java/com/tripplanner/trip/domain/ItineraryDay.java` exists
- ItineraryDay.java contains `@Table(name = "itinerary_days", schema = "trip")`
- ItineraryDay.java contains `private UUID tripId` with `updatable = false`
- File `services/trip-service/src/main/java/com/tripplanner/trip/domain/ItineraryItem.java` exists
- ItineraryItem.java contains `@Table(name = "itinerary_items", schema = "trip")`
- ItineraryItem.java contains `private UUID itineraryDayId`
</acceptance_criteria>
</task>

### Task 2: Create repositories + exception classes + DTOs

<task>
<read_first>
- services/auth-service/src/main/java/com/tripplanner/auth/repository/UserRepository.java (repo analog)
- services/auth-service/src/main/java/com/tripplanner/auth/repository/RefreshTokenRepository.java (analog for @Modifying @Query pattern)
- services/auth-service/src/main/java/com/tripplanner/auth/service/exception/InvalidCredentialsException.java (exception pattern — skinny, no-arg)
- services/auth-service/src/main/java/com/tripplanner/auth/api/dto/SignupRequest.java (DTO pattern)
- services/auth-service/src/main/java/com/tripplanner/auth/api/dto/LoginResponse.java (response DTO pattern)
</read_first>

<action>
**A) Create `services/trip-service/src/main/java/com/tripplanner/trip/repository/TripRepository.java`:**
```java
package com.tripplanner.trip.repository;

import com.tripplanner.trip.domain.Trip;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

import java.util.Optional;
import java.util.UUID;

@Repository
public interface TripRepository extends JpaRepository<Trip, UUID> {
    Optional<Trip> findByIdAndUserId(UUID id, UUID userId);
    Page<Trip> findByUserIdOrderByCreatedAtDesc(UUID userId, Pageable pageable);
}
```

Per D-09: `findByIdAndUserId` enforces ownership. Per D-05: sorted `created_at DESC`.

**B) Create `services/trip-service/src/main/java/com/tripplanner/trip/repository/ItineraryDayRepository.java`:**
```java
package com.tripplanner.trip.repository;

import com.tripplanner.trip.domain.ItineraryDay;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Modifying;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;
import org.springframework.stereotype.Repository;

import java.time.LocalDate;
import java.util.List;
import java.util.UUID;

@Repository
public interface ItineraryDayRepository extends JpaRepository<ItineraryDay, UUID> {
    List<ItineraryDay> findByTripIdOrderByDayIndex(UUID tripId);
    List<ItineraryDay> findByTripIdAndDayDateIn(UUID tripId, List<LocalDate> dates);
    void deleteByTripIdAndIdIn(UUID tripId, List<UUID> ids);

    @Modifying
    @Query("UPDATE ItineraryDay d SET d.dayIndex = :newIndex WHERE d.id = :id")
    void updateDayIndex(@Param("id") UUID id, @Param("newIndex") int newIndex);
}
```

**C) Create `services/trip-service/src/main/java/com/tripplanner/trip/repository/ItineraryItemRepository.java`:**
```java
package com.tripplanner.trip.repository;

import com.tripplanner.trip.domain.ItineraryItem;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Modifying;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;
import org.springframework.stereotype.Repository;

import java.util.List;
import java.util.UUID;

@Repository
public interface ItineraryItemRepository extends JpaRepository<ItineraryItem, UUID> {

    @Query("SELECT COUNT(i) FROM ItineraryItem i WHERE i.itineraryDayId IN :dayIds")
    long countByDayIds(@Param("dayIds") List<UUID> dayIds);

    @Modifying(flushAutomatically = true, clearAutomatically = true)
    @Query("DELETE FROM ItineraryItem i WHERE i.itineraryDayId IN :dayIds")
    void deleteByDayIds(@Param("dayIds") List<UUID> dayIds);
}
```

Per D-02: explicit JPQL DELETE (not JPA CascadeType.REMOVE). Per D-03: `countByDayIds` for shrink-conflict check.

**D) Create exception classes:**

`services/trip-service/src/main/java/com/tripplanner/trip/service/exception/TripNotFoundException.java`:
```java
package com.tripplanner.trip.service.exception;

public class TripNotFoundException extends RuntimeException {
    public TripNotFoundException() { super(); }
}
```

`services/trip-service/src/main/java/com/tripplanner/trip/service/exception/InvalidDateRangeException.java`:
```java
package com.tripplanner.trip.service.exception;

public class InvalidDateRangeException extends RuntimeException {
    public InvalidDateRangeException() { super(); }
}
```

`services/trip-service/src/main/java/com/tripplanner/trip/service/exception/ShortenConflictException.java`:
```java
package com.tripplanner.trip.service.exception;

import java.time.LocalDate;
import java.util.List;

public class ShortenConflictException extends RuntimeException {

    public record OrphanedDayInfo(LocalDate dayDate, int dayIndex, long itemCount) {}

    private final List<OrphanedDayInfo> orphanedDays;

    public ShortenConflictException(List<OrphanedDayInfo> orphanedDays) {
        super();
        this.orphanedDays = orphanedDays;
    }

    public List<OrphanedDayInfo> getOrphanedDays() { return orphanedDays; }
}
```

Per D-06: carries `orphanedDays` list for the 409 response body (dayDate, dayIndex, itemCount).

**E) Create DTO records:**

`services/trip-service/src/main/java/com/tripplanner/trip/api/dto/CreateTripRequest.java`:
```java
package com.tripplanner.trip.api.dto;

import jakarta.validation.constraints.NotBlank;
import jakarta.validation.constraints.Size;
import java.time.LocalDate;

public record CreateTripRequest(
        @NotBlank @Size(max = 120) String name,
        LocalDate startDate,
        LocalDate endDate
) {}
```

Per D-13: name 1–120, trimmed, reject blank. Dates are nullable.

`services/trip-service/src/main/java/com/tripplanner/trip/api/dto/UpdateTripRequest.java`:
```java
package com.tripplanner.trip.api.dto;

import jakarta.validation.constraints.Size;
import java.time.LocalDate;

public record UpdateTripRequest(
        @Size(min = 1, max = 120) String name,
        LocalDate startDate,
        LocalDate endDate,
        @Size(max = 2048) String coverImageUrl
) {}
```

Per RESEARCH §4 PATCH semantics: null = no change. `@Size(min = 1)` on name prevents blank when provided, but allows null.

`services/trip-service/src/main/java/com/tripplanner/trip/api/dto/TripResponse.java`:
```java
package com.tripplanner.trip.api.dto;

import com.tripplanner.trip.domain.ItineraryDay;
import com.tripplanner.trip.domain.Trip;

import java.time.Instant;
import java.time.LocalDate;
import java.util.List;
import java.util.UUID;

public record TripResponse(
        UUID id, String name, LocalDate startDate, LocalDate endDate,
        String coverImageUrl, Instant createdAt, Instant updatedAt,
        List<DayResponse> days
) {
    public static TripResponse from(Trip trip, List<ItineraryDay> days) {
        List<DayResponse> dayResponses = days == null ? List.of()
                : days.stream()
                    .map(d -> new DayResponse(d.getId(), d.getDayDate(), d.getDayIndex()))
                    .toList();
        return new TripResponse(
                trip.getId(), trip.getName(), trip.getStartDate(), trip.getEndDate(),
                trip.getCoverImageUrl(), trip.getCreatedAt(), trip.getUpdatedAt(),
                dayResponses
        );
    }
}
```

Per D-08: detail response includes embedded `days[]` array. Static factory `from()` for mapping.

`services/trip-service/src/main/java/com/tripplanner/trip/api/dto/TripListResponse.java`:
```java
package com.tripplanner.trip.api.dto;

import java.util.List;

public record TripListResponse(
        List<TripSummaryResponse> content,
        long totalElements, int totalPages, int page, int size
) {
    public record TripSummaryResponse(
            java.util.UUID id, String name, java.time.LocalDate startDate,
            java.time.LocalDate endDate, String coverImageUrl,
            java.time.Instant createdAt, java.time.Instant updatedAt
    ) {}
}
```

Per D-07: Spring Data Page convention shape with `content`, `totalElements`, `totalPages`, `page`, `size`.

`services/trip-service/src/main/java/com/tripplanner/trip/api/dto/DayResponse.java`:
```java
package com.tripplanner.trip.api.dto;

import java.time.LocalDate;
import java.util.UUID;

public record DayResponse(UUID id, LocalDate dayDate, int dayIndex) {}
```
</action>

<acceptance_criteria>
- `TripRepository.java` contains `Optional<Trip> findByIdAndUserId(UUID id, UUID userId)`
- `TripRepository.java` contains `Page<Trip> findByUserIdOrderByCreatedAtDesc`
- `ItineraryDayRepository.java` contains `List<ItineraryDay> findByTripIdOrderByDayIndex(UUID tripId)`
- `ItineraryDayRepository.java` contains `void updateDayIndex(@Param("id") UUID id, @Param("newIndex") int newIndex)`
- `ItineraryItemRepository.java` contains `long countByDayIds(@Param("dayIds") List<UUID> dayIds)`
- `ItineraryItemRepository.java` contains `@Modifying` and `void deleteByDayIds`
- `TripNotFoundException.java` exists with no-arg constructor
- `InvalidDateRangeException.java` exists with no-arg constructor
- `ShortenConflictException.java` contains `record OrphanedDayInfo(LocalDate dayDate, int dayIndex, long itemCount)`
- `ShortenConflictException.java` contains `List<OrphanedDayInfo> getOrphanedDays()`
- `CreateTripRequest.java` contains `@NotBlank @Size(max = 120) String name`
- `UpdateTripRequest.java` contains `@Size(min = 1, max = 120) String name`
- `TripResponse.java` contains `public static TripResponse from(Trip trip, List<ItineraryDay> days)`
- `TripListResponse.java` contains `record TripSummaryResponse`
- `DayResponse.java` contains `record DayResponse(UUID id, LocalDate dayDate, int dayIndex)`
- `./gradlew :services:trip-service:compileJava` exits 0
</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | DTOs are the trust boundary — Bean Validation annotations enforce input constraints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Spoofing | TripRepository findByIdAndUserId | mitigate | All ownership queries filter by userId + id — no separate authz check needed (D-09) |
| T-05-05 | Tampering | CreateTripRequest | mitigate | `@NotBlank @Size(max = 120)` prevents empty/oversize names; `@Valid` on controller enforces |
| T-05-06 | Tampering | UpdateTripRequest — mass assignment | mitigate | DTO has only `name`, `startDate`, `endDate`, `coverImageUrl` — no `id` or `userId` field; service layer ignores unknown fields |
| T-05-07 | Info Disclosure | TripNotFoundException | mitigate | Returns 404 (not 403) — no information about whether the trip exists for other users (D-09) |
| T-05-08 | Injection | JPQL queries in repositories | mitigate | All queries use parameterized `@Param` bindings — no string concatenation |
</threat_model>

<verification>
```bash
./gradlew :services:trip-service:compileJava
```
All 14 files compile. Entity annotations match V2/V3/V4 DDL column types byte-for-byte.
</verification>

<success_criteria>
- 3 entities, 3 repositories, 3 exceptions, 5 DTOs all compile
- Repository methods support ownership-filtered queries
- ShortenConflictException carries orphan info
- DTOs enforce validation constraints
- No `CascadeType.REMOVE` in any entity (per D-02)
</success_criteria>

<output>
After completion, create `.planning/phases/05-trip-service-trips-days/05-02-SUMMARY.md`
</output>
