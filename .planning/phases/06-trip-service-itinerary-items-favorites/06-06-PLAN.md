---
phase: 06-trip-service-itinerary-items-favorites
plan: 06
type: execute
wave: 4
depends_on: [03, 04, 05]
files_modified:
  - services/trip-service/src/test/java/com/tripplanner/trip/api/ItemControllerIT.java
  - services/trip-service/src/test/java/com/tripplanner/trip/api/FavoriteControllerIT.java
  - services/trip-service/src/test/java/com/tripplanner/trip/service/ItineraryItemServiceTest.java
  - services/trip-service/src/test/java/com/tripplanner/trip/service/FavoriteServiceTest.java
autonomous: true
requirements:
  - TRIP-03
  - TRIP-04
  - PERS-01
  - PERS-02
  - PERS-03
  - NFR-06
must_haves:
  truths:
    - "Integration tests verify full HTTP round-trip for item CRUD"
    - "Integration tests verify XSS sanitization (script tags stripped)"
    - "Integration tests verify favorites idempotent add (201 then 200)"
    - "Unit tests verify position logic (append at MAX+100, reindex)"
    - "Unit tests verify cross-day move validation"
  artifacts:
    - path: "services/trip-service/src/test/java/com/tripplanner/trip/api/ItemControllerIT.java"
      provides: "Integration tests for item endpoints"
    - path: "services/trip-service/src/test/java/com/tripplanner/trip/api/FavoriteControllerIT.java"
      provides: "Integration tests for favorites endpoints"
    - path: "services/trip-service/src/test/java/com/tripplanner/trip/service/ItineraryItemServiceTest.java"
      provides: "Unit tests for item service with position/sanitization logic"
    - path: "services/trip-service/src/test/java/com/tripplanner/trip/service/FavoriteServiceTest.java"
      provides: "Unit tests for favorite service"
  key_links:
    - from: "ItemControllerIT"
      to: "TripIntegrationTestBase"
      via: "extends base class for Testcontainers"
      pattern: "extends TripIntegrationTestBase"
---

<objective>
Create integration tests (Testcontainers, full HTTP) for item and favorite endpoints, plus unit tests for ItineraryItemService (position logic, sanitization) and FavoriteService (idempotent behavior).

Purpose: Verify all 5 success criteria are met. Tests are the phase's quality gate.
Output: 4 test files covering CRUD, XSS prevention, position algorithm, and idempotent favorites.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-trip-service-itinerary-items-favorites/06-CONTEXT.md
@.planning/ROADMAP.md
</context>

<interfaces>
<!-- Test infrastructure from Phase 5 -->

From services/trip-service/src/test/java/com/tripplanner/trip/support/TripIntegrationTestBase.java:
```java
// Provides: singleton PostgreSQLContainer, JWT fixtures, TestRestTemplate/MockMvc, application-test.yml
// Tests extend this class for full HTTP round-trip with real DB
```

From libs/jwt-common/src/testFixtures/java/com/tripplanner/jwt/JwtFixtures.java:
```java
public static String mintAccessToken(String userId)  // Creates valid JWT for test user
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Integration tests for item and favorite endpoints</name>
  <files>
    services/trip-service/src/test/java/com/tripplanner/trip/api/ItemControllerIT.java
    services/trip-service/src/test/java/com/tripplanner/trip/api/FavoriteControllerIT.java
  </files>
  <read_first>
    - services/trip-service/src/test/java/com/tripplanner/trip/api/TripControllerIT.java (IT pattern — extend TripIntegrationTestBase, use MockMvc)
    - services/trip-service/src/test/java/com/tripplanner/trip/support/TripIntegrationTestBase.java (base class setup)
    - services/trip-service/src/test/resources/application-test.yml (test config)
    - docs/04-api-spec.md (expected status codes and response shapes)
  </read_first>
  <action>
1. Create ItemControllerIT.java — extends TripIntegrationTestBase:
   - Helper methods: createTrip(), addDays() using direct MockMvc calls to existing trip endpoints
   - **Test: addItem_appendsWithCorrectPosition** — Add 3 items to a day, verify positions are 100, 200, 300
   - **Test: addItem_sanitizesNote** — Add item with note `<script>alert(1)</script>`, verify stored/returned note is `alert(1)` (SC-3 / NFR-06)
   - **Test: patchItem_movesToNewPosition** — Add 3 items, PATCH second to position 350, verify order
   - **Test: patchItem_crossDayMove** — Move item from day 1 to day 2, verify itineraryDayId changes
   - **Test: patchItem_dayNotInTrip_returns400** — PATCH with dayId from another trip, expect 400 with code `trip.day_not_in_trip`
   - **Test: deleteItem_returns204** — Delete an item, verify 204 and item gone
   - **Test: deleteItem_notOwned_returns404** — Try to delete item from another user's trip, expect 404
   - **Test: addItem_unauthenticated_returns401** — No JWT, expect 401

2. Create FavoriteControllerIT.java — extends TripIntegrationTestBase:
   - **Test: addFavorite_returns201** — POST /api/favorites, verify 201 + response body
   - **Test: addFavorite_idempotent_returns200** — POST same ref twice, first 201, second 200 (SC-4 partial)
   - **Test: removeFavorite_returns204** — DELETE /api/favorites/{ref}, verify 204
   - **Test: listFavorites_returnsAll** — Add 3 favorites, GET /api/favorites, verify all 3 in response
   - **Test: listFavorites_emptyReturnsEmptyList** — GET with no favorites, verify `{"items":[]}`
   - **Test: favorites_unauthenticated_returns401** — No JWT, expect 401
  </action>
  <acceptance_criteria>
    - ItemControllerIT.java extends TripIntegrationTestBase
    - Contains test verifying position 100, 200, 300 for sequential adds
    - Contains test verifying `<script>alert(1)</script>` is stripped to `alert(1)`
    - Contains test verifying cross-day move
    - Contains test verifying 400 for day not in trip
    - FavoriteControllerIT.java extends TripIntegrationTestBase
    - Contains test verifying 201 then 200 for idempotent add
    - Contains test verifying empty list response
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/huyqtrieu/Desktop/Practice/java-lab && ./gradlew :services:trip-service:compileTestJava --no-daemon -q 2>&1; echo "EXIT:$?"</automated>
  </verify>
  <done>Integration tests compile and cover item CRUD + XSS sanitization + favorites idempotency</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for ItineraryItemService and FavoriteService</name>
  <files>
    services/trip-service/src/test/java/com/tripplanner/trip/service/ItineraryItemServiceTest.java
    services/trip-service/src/test/java/com/tripplanner/trip/service/FavoriteServiceTest.java
  </files>
  <read_first>
    - services/trip-service/src/test/java/com/tripplanner/trip/service/TripServiceTest.java (unit test pattern — @ExtendWith(MockitoExtension.class))
    - services/trip-service/src/test/java/com/tripplanner/trip/service/DayMaterializationServiceTest.java (Mockito mock pattern)
    - services/trip-service/src/main/java/com/tripplanner/trip/service/ItineraryItemService.java (methods to test)
    - services/trip-service/src/main/java/com/tripplanner/trip/service/FavoriteService.java (methods to test)
  </read_first>
  <action>
1. Create ItineraryItemServiceTest.java with @ExtendWith(MockitoExtension.class):
   - Mock TripRepository, ItineraryDayRepository, ItineraryItemRepository
   - **Test: addItem_firstItem_getsPosition100** — verify MAX returns empty → position = 100
   - **Test: addItem_appendsAtMaxPlus100** — verify MAX returns 300 → position = 400
   - **Test: addItem_sanitizesHtmlFromNote** — verify `<b>bold</b>` becomes `bold`, `<script>x</script>` becomes `x`
   - **Test: addItem_nullNote_remainsNull** — null note stays null
   - **Test: addItem_tripNotOwned_throws** — tripRepo returns empty → TripNotFoundException
   - **Test: addItem_dayNotInTrip_throws** — day.tripId != tripId → DayNotInTripException
   - **Test: updateItem_crossDayMove_updatesDay** — verify item.setItineraryDayId called with new dayId
   - **Test: updateItem_invalidTargetDay_throws** — target day not in trip → DayNotInTripException
   - **Test: updateItem_itemNotFound_throws** — item not in any day of trip → ItemNotFoundException
   - **Test: deleteItem_removesItem** — verify delete called
   - **Test: deleteItem_notOwned_throws** — trip not found → TripNotFoundException

2. Create FavoriteServiceTest.java with @ExtendWith(MockitoExtension.class):
   - Mock FavoriteRepository
   - **Test: addFavorite_new_createsAndReturnsCreatedTrue** — findByUserIdAndDestinationRef returns empty → save called, result.created() = true
   - **Test: addFavorite_existing_returnsCreatedFalse** — findByUserIdAndDestinationRef returns present → no save, result.created() = false
   - **Test: removeFavorite_callsDelete** — verify deleteByUserIdAndDestinationRef called
   - **Test: listFavorites_returnsOrdered** — verify returns from repository
  </action>
  <acceptance_criteria>
    - ItineraryItemServiceTest.java uses @ExtendWith(MockitoExtension.class) with @Mock annotations
    - Contains test verifying position 100 for first item
    - Contains test verifying HTML stripping (Jsoup sanitization)
    - Contains test verifying DayNotInTripException for invalid day
    - Contains test verifying TripNotFoundException for unowned trip
    - FavoriteServiceTest.java tests idempotent behavior (created=true vs created=false)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/huyqtrieu/Desktop/Practice/java-lab && ./gradlew :services:trip-service:test --no-daemon -q --tests "com.tripplanner.trip.service.ItineraryItemServiceTest" --tests "com.tripplanner.trip.service.FavoriteServiceTest" 2>&1 | tail -20</automated>
  </verify>
  <done>Unit tests pass — verify position algorithm, sanitization, ownership checks, and idempotent favorites</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test→production code | Tests verify security boundaries (auth, ownership) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-14 | Tampering | XSS test | mitigate | Integration test verifies script tag stripped — regression gate |
| T-06-15 | Elevation | ownership test | mitigate | IT verifies cross-user returns 404 — regression gate |
</threat_model>

<verification>
- Unit tests pass: `./gradlew :services:trip-service:test --tests "*.service.*Test" --no-daemon -q`
- Integration tests compile: `./gradlew :services:trip-service:compileTestJava --no-daemon -q`
- (Full IT run requires Docker — Testcontainers)
</verification>

<success_criteria>
All unit tests pass. Integration tests compile (full run needs Docker). Tests cover all 5 phase success criteria: position append, position reorder, XSS strip, favorites idempotent, ownership enforcement.
</success_criteria>

<output>
After completion, create `.planning/phases/06-trip-service-itinerary-items-favorites/06-06-SUMMARY.md`
</output>
