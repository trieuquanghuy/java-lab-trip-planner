---
phase: 07-frontend-auth-discovery
plan: 05
type: execute
wave: 3
depends_on: [07-02, 07-04]
files_modified:
  - frontend/src/pages/DestinationDetailPage.tsx
  - frontend/src/features/destinations/PhotoCarousel.tsx
  - frontend/src/App.tsx
autonomous: true
requirements: [TRIP-05]
must_haves:
  truths:
    - "Destination detail page shows name, category, description, photos, address, hours, website"
    - "Missing data is handled gracefully with explicit placeholder text"
    - "Photo carousel scrolls horizontally with dot indicators; placeholder when no photos"
    - "Add to Trip button disabled with popover when user is logged out (D-14)"
    - "Add to Trip button triggers deferred login intent when clicked while logged out (D-14/D-15)"
  artifacts:
    - path: "frontend/src/pages/DestinationDetailPage.tsx"
      provides: "Full destination detail view with all fields and Add to Trip CTA"
    - path: "frontend/src/features/destinations/PhotoCarousel.tsx"
      provides: "Horizontal photo scroll with dots indicator and placeholder"
  key_links:
    - from: "frontend/src/pages/DestinationDetailPage.tsx"
      to: "frontend/src/features/destinations/destinations.api.ts"
      via: "useQuery fetches detail by providerRef"
      pattern: "useQuery.*destination.*providerRef"
    - from: "frontend/src/pages/DestinationDetailPage.tsx"
      to: "frontend/src/features/auth/auth.store.ts"
      via: "checks isAuthenticated for Add to Trip button state"
      pattern: "useAuthStore|useAuth"
---

<objective>
Build the destination detail page with photo carousel, all info fields with graceful missing-data handling, and the "Add to Trip" CTA with deferred login intent for logged-out users (TRIP-05 core flow).

Purpose: Implements D-10 through D-15 — destination presentation and the critical deferred intent that is the phase requirement TRIP-05.
Output: DestinationDetailPage, PhotoCarousel component.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-frontend-auth-discovery/07-CONTEXT.md
@.planning/phases/07-frontend-auth-discovery/07-RESEARCH.md
@frontend/src/types/api.ts (from Plan 01)
@frontend/src/features/auth/useAuth.ts (from Plan 02)
@frontend/src/features/destinations/destinations.api.ts (from Plan 04)

<interfaces>
From Plan 01 types:
  DestinationDetailResponse: { providerRef, name, category, shortDescription, rating, lat, lng, address, website, photos: string[], openingHours: Record<string,string>|null, fromCache, fetchedAt }

From Plan 02 auth:
  useAuth() → { isAuthenticated, setAddToTripContext }
  useAuthStore — addToTripContext: { destinationRef: string } | null

From Plan 04 destinations.api:
  fetchDestinationDetail(providerRef: string) → DestinationDetailResponse

Backend endpoint:
  GET /api/destinations/:providerRef → DestinationDetailResponse | 404
  providerRef format: "otm:xxx" or "fsq:xxx"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PhotoCarousel component</name>
  <files>frontend/src/features/destinations/PhotoCarousel.tsx</files>
  <read_first>frontend/src/types/api.ts</read_first>
  <action>
Create `src/features/destinations/PhotoCarousel.tsx`:
- Props: `photos: string[]`
- State: `currentIndex: number` (for dot indicator)
- If `photos.length === 0`: render a placeholder div:
  - `h-64 w-full bg-gradient-to-br from-blue-400 to-purple-500 rounded-lg flex items-center justify-center`
  - Inside: lucide-react `MapPin` icon (size 48, white) + "No photos available" text (text-white/80)
- If photos exist:
  - Container: `relative overflow-hidden rounded-lg`
  - Scrollable area: `flex overflow-x-auto snap-x snap-mandatory gap-0` with `scrollbar-hide` (use `-webkit-overflow-scrolling: touch` for mobile)
  - Each photo: `<img src={url} alt="Destination photo" className="w-full h-64 object-cover flex-shrink-0 snap-center" loading="lazy" />`
  - On scroll: update currentIndex by detecting which image is in view (use IntersectionObserver or scroll position calculation)
  - Dots indicator: absolute bottom-2 left-1/2 -translate-x-1/2, row of small circles (w-2 h-2 rounded-full), active = bg-white, inactive = bg-white/50
- Per D-11: horizontal scroll with dots indicator
- Per D-12: placeholder gradient when no photos
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - PhotoCarousel renders gradient placeholder div with MapPin icon when photos is empty
    - PhotoCarousel renders scrollable image list with snap-x when photos exist
    - Dot indicators rendered at bottom (one per photo), active dot has bg-white
    - Images use loading="lazy" attribute
    - tsc --noEmit passes
  </acceptance_criteria>
  <done>Photo carousel handles both empty and populated photo arrays with scroll + dots UI</done>
</task>

<task type="auto">
  <name>Task 2: DestinationDetailPage + wire route + Add to Trip CTA (TRIP-05)</name>
  <files>frontend/src/pages/DestinationDetailPage.tsx, frontend/src/App.tsx</files>
  <read_first>frontend/src/features/destinations/destinations.api.ts, frontend/src/features/auth/useAuth.ts, frontend/src/features/auth/auth.store.ts, frontend/src/features/destinations/PhotoCarousel.tsx, frontend/src/components/ui/popover.tsx, frontend/src/components/ui/button.tsx, frontend/src/components/ui/skeleton.tsx, frontend/src/components/ui/badge.tsx</read_first>
  <action>
1. Create `src/pages/DestinationDetailPage.tsx`:
   - Import: `useParams`, `useNavigate` from react-router-dom
   - Import: `useQuery` from @tanstack/react-query
   - Import: `fetchDestinationDetail` from destinations.api
   - Import: `useAuth` from @/features/auth/useAuth
   - Import: `PhotoCarousel` from @/features/destinations/PhotoCarousel
   - Import: `Button`, `Badge`, `Skeleton`, `Popover`, `PopoverTrigger`, `PopoverContent` from shadcn
   - Import: `toast` from sonner
   - Import: `MapPin`, `Globe`, `Clock`, `Star` from lucide-react

   - Get `providerRef` from `useParams()`
   - Query: `useQuery({ queryKey: ['destination', providerRef], queryFn: () => fetchDestinationDetail(providerRef!), enabled: !!providerRef })`
   - Get `{ isAuthenticated, setAddToTripContext }` from `useAuth()`

   - Loading state: full-page skeleton (photo area skeleton + text skeletons)
   - Error/not found: "Destination not found" message with back link

   - Layout (per D-10, full routed page):
     - `<PhotoCarousel photos={data.photos} />`
     - Below photos:
       - Name: `text-2xl font-bold`
       - Category: `<Badge>` if not null
       - Rating: `★ {rating}` if not null (with Star icon, yellow)
       - Description: `text-muted-foreground` paragraph; if null: "No description available" in muted/italic text (D-12)
       - Address: with MapPin icon; if null: omit section entirely
       - Opening hours: with Clock icon; if null: "Opening hours not available" explicit text (D-12)
         - If present (object): render as key-value list (day: hours)
       - Website: with Globe icon as an external link `<a href={website} target="_blank" rel="noopener noreferrer">`; if null: omit
     - "Add to Trip" CTA button (per D-13: prominent on desktop, sticky-bottom on mobile):
       - Desktop: `<Button size="lg">` in the detail layout
       - Mobile: `fixed bottom-0 left-0 right-0 p-4 bg-background border-t md:relative md:border-0 md:p-0 md:mt-6`
       - If `isAuthenticated`:
         - Button is enabled but clicking shows toast "Trip management coming soon" (Phase 8 implements the actual dialog)
       - If NOT authenticated (D-14):
         - Button wrapped in Popover:
           - PopoverTrigger: the Button (visually enabled but triggers popover)
           - PopoverContent: "Log in to add destinations to your trip" text + "Log in" Button
           - "Log in" button onClick:
             ```ts
             setAddToTripContext({ destinationRef: providerRef! });
             navigate('/login');
             ```
         - This implements D-14 (tooltip/popover) + D-15 (stores intent in Zustand before redirecting)

2. Update `src/App.tsx` — replace DestinationDetail placeholder with real import:
   - Import `{ DestinationDetailPage }` from `@/pages/DestinationDetailPage`
   - Replace `<PlaceholderPage name="Destination Detail" />` with `<DestinationDetailPage />`
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - DestinationDetailPage uses useQuery with key ['destination', providerRef]
    - Page renders PhotoCarousel, name, category Badge, rating with Star icon, description (or "No description available"), address (or omitted), opening hours (or "Opening hours not available"), website link (or omitted)
    - "Add to Trip" button uses Popover when not authenticated: content says "Log in to add destinations to your trip" with a "Log in" button
    - Popover's "Log in" button calls setAddToTripContext({ destinationRef }) and navigates to /login
    - Mobile sticky CTA: element has classes containing "fixed bottom-0" and "md:relative"
    - App.tsx imports DestinationDetailPage for the /destinations/:providerRef route
    - tsc --noEmit passes
  </acceptance_criteria>
  <done>Destination detail page renders all fields with graceful missing-data handling; TRIP-05 deferred login intent implemented via popover + Zustand context storage + redirect to login</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL param → API call | providerRef from URL used in API request |
| Backend response → DOM | Destination data rendered in JSX (React auto-escapes) |
| User click → state mutation | "Add to Trip" stores intent in Zustand |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-15 | T (Tampering) | providerRef URL param | mitigate | Backend validates format (^(otm|fsq):[a-zA-Z0-9_-]+$); frontend receives 400/404 for invalid refs |
| T-07-16 | I (Info Disclosure) | DestinationDetail | accept | Destination data is public; no auth required to view |
| T-07-17 | S (Spoofing) | Add to Trip intent | accept | Zustand state is ephemeral (memory only); actual trip mutation happens in Phase 8 with full auth check server-side |
</threat_model>

<verification>
- `pnpm tsc --noEmit` passes
- Navigating to `/destinations/otm:123` shows detail page with all sections
- Missing fields show placeholder text (not crash)
- Logged-out: clicking "Add to Trip" shows popover with "Log in" button
- Clicking "Log in" in popover navigates to `/login` and stores destinationRef in Zustand
</verification>

<success_criteria>
Destination detail page complete with photo carousel, all info fields, graceful empty handling, and TRIP-05 deferred login intent via popover → Zustand → login redirect.
</success_criteria>

<output>
After completion, create `.planning/phases/07-frontend-auth-discovery/07-05-SUMMARY.md`
</output>
