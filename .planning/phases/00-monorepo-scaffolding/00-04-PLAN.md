---
phase: 00-monorepo-scaffolding
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - libs/error-handling/build.gradle.kts
  - libs/error-handling/src/main/java/com/tripplanner/errors/ProblemDetailFactory.java
  - libs/error-handling/src/main/java/com/tripplanner/errors/ErrorCode.java
  - libs/api-contracts/build.gradle.kts
  - libs/api-contracts/src/main/java/.gitkeep
autonomous: true
requirements: [NFR-04]

must_haves:
  truths:
    - "`libs/error-handling` ships ONLY `ProblemDetailFactory` static helpers and the 2-baseline-code `ErrorCode` enum (D-05). No `GlobalExceptionHandler` or `@ControllerAdvice` in Phase 0."
    - "`libs/api-contracts` is an empty Gradle module — folder + `build.gradle.kts` + `.gitkeep` only; no source files (D-06)."
    - "`libs/jwt-common` directory does NOT exist after Phase 0 — Phase 1 owns its creation (D-07 / C22)."
  artifacts:
    - path: "libs/error-handling/build.gradle.kts"
      provides: "java-library exposing org.springframework:spring-web (for ProblemDetail) via api configuration"
      exports: ["api(\"org.springframework:spring-web\")"]
    - path: "libs/error-handling/src/main/java/com/tripplanner/errors/ProblemDetailFactory.java"
      provides: "Static helpers returning ProblemDetail with stable type URI + code extension"
      contains: "ProblemDetail.forStatusAndDetail"
    - path: "libs/error-handling/src/main/java/com/tripplanner/errors/ErrorCode.java"
      provides: "Enum with 2 baseline codes only: AUTH_UNAUTHORIZED, AUTH_RATE_LIMITED"
      contains: "AUTH_UNAUTHORIZED"
    - path: "libs/api-contracts/build.gradle.kts"
      provides: "Empty java-library module so settings.gradle.kts include compiles"
    - path: "libs/api-contracts/src/main/java/.gitkeep"
      provides: "Forces git to track the empty source dir so Phase 1 can drop UserContext.java in"
  key_links:
    - from: "Phase 1 gateway error WebFilter (future)"
      to: "libs/error-handling/ProblemDetailFactory"
      via: "ProblemDetailFactory.of(HttpStatus.UNAUTHORIZED, ErrorCode.AUTH_UNAUTHORIZED, detail)"
      pattern: "ProblemDetailFactory\\.of"
    - from: "Phase 1 UserContext record (future)"
      to: "libs/api-contracts/src/main/java/com/tripplanner/contracts/"
      via: "new file added in Phase 1 plan; module already wired in settings.gradle.kts"
      pattern: "package com.tripplanner.contracts"
---

<objective>
Build `libs/error-handling` (minimal stubs per D-05) and `libs/api-contracts` (empty module per D-06). These are the two libs that Phase 0 ships in **stub form** so that Phase 1 can add types/exception handlers without touching `settings.gradle.kts` or any service `build.gradle.kts` — the dependency graph is already wired.

Purpose: Lock the 2-baseline `ErrorCode` enum and the `ProblemDetailFactory` shape so Phase 1's gateway error filter and Phase 2's auth-service exceptions map to a consistent RFC 7807 envelope from day one. Reserve the `libs/api-contracts` folder so Phase 1's `UserContext` record drops in cleanly.

Output: Two `java-library` Gradle subprojects: `error-handling` with two `.java` source files (factory + enum), and `api-contracts` with no source files (just a `.gitkeep`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-monorepo-scaffolding/00-CONTEXT.md
@.planning/phases/00-monorepo-scaffolding/00-RESEARCH.md
@.planning/phases/00-monorepo-scaffolding/00-PATTERNS.md
@docs/04-api-spec.md
@gradle/libs.versions.toml
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 4.1: Create libs/error-handling — build file + ProblemDetailFactory + ErrorCode enum</name>
  <files>libs/error-handling/build.gradle.kts, libs/error-handling/src/main/java/com/tripplanner/errors/ProblemDetailFactory.java, libs/error-handling/src/main/java/com/tripplanner/errors/ErrorCode.java</files>
  <read_first>
    - .planning/phases/00-monorepo-scaffolding/00-CONTEXT.md (lines 71-74 — D-05 — 2 baseline codes only, no GlobalExceptionHandler)
    - .planning/phases/00-monorepo-scaffolding/00-PATTERNS.md (lines 314-376 — Bucket B `error-handling/build.gradle.kts`, ProblemDetailFactory, ErrorCode excerpts)
    - docs/04-api-spec.md §6 — error code catalog (only the 2 baseline codes apply in Phase 0)
  </read_first>
  <action>
    1. **`libs/error-handling/build.gradle.kts`** verbatim from PATTERNS.md Bucket B:
       ```kotlin
       plugins {
           `java-library`
           alias(libs.plugins.spring.dependency.management)
       }

       dependencies {
           api("org.springframework:spring-web")  // for ProblemDetail
       }

       dependencyManagement {
           imports {
               mavenBom("org.springframework.boot:spring-boot-dependencies:${libs.versions.springBoot.get()}")
           }
       }
       ```
       The `spring-web` dep brings `org.springframework.http.ProblemDetail` (Spring 6.0+). `api` because consumers need to return `ProblemDetail` from controllers. Header comment: `// D-05: minimal stub. No GlobalExceptionHandler or @ControllerAdvice — those are added per service in Phase 1+ when real endpoints exist.`

    2. **`ProblemDetailFactory.java`** verbatim from PATTERNS.md Bucket B (lines 336-358):
       ```java
       package com.tripplanner.errors;

       import org.springframework.http.HttpStatus;
       import org.springframework.http.ProblemDetail;

       import java.net.URI;

       public final class ProblemDetailFactory {

           private static final String TYPE_BASE = "https://tripplanner.example.com/errors/";

           private ProblemDetailFactory() {}

           public static ProblemDetail of(HttpStatus status, ErrorCode code, String detail) {
               ProblemDetail pd = ProblemDetail.forStatusAndDetail(status, detail);
               pd.setType(URI.create(TYPE_BASE + code.code()));
               pd.setProperty("code", code.code());
               return pd;
           }
       }
       ```
       Per docs/04-api-spec.md §6 the `code` extension member is the stable machine-readable identifier consumers parse; the `type` URI is the human-readable doc reference. This factory is the single point that produces both consistently.

    3. **`ErrorCode.java`** verbatim from PATTERNS.md Bucket B (lines 360-376) — **EXACTLY 2 baseline codes per D-05**:
       ```java
       package com.tripplanner.errors;

       public enum ErrorCode {
           AUTH_UNAUTHORIZED("auth.unauthorized"),
           AUTH_RATE_LIMITED("auth.rate_limited");

           private final String code;

           ErrorCode(String code) { this.code = code; }

           public String code() { return code; }
       }
       ```
       **Do NOT add additional codes in Phase 0.** Phase 1+ extends this enum as new codes arise. The 2 codes shipping now are exactly the ones the gateway will need in Phase 1 for JWT validation failures and login rate-limit responses. Adding a third code now would be scope creep and contradict D-05.

    All files in package `com.tripplanner.errors` (D-28 / C1).
  </action>
  <verify>
    <automated>test -f libs/error-handling/build.gradle.kts && grep -q 'java-library' libs/error-handling/build.gradle.kts && grep -q 'spring-web' libs/error-handling/build.gradle.kts && test -f libs/error-handling/src/main/java/com/tripplanner/errors/ProblemDetailFactory.java && grep -q 'package com.tripplanner.errors' libs/error-handling/src/main/java/com/tripplanner/errors/ProblemDetailFactory.java && grep -q 'ProblemDetail.forStatusAndDetail' libs/error-handling/src/main/java/com/tripplanner/errors/ProblemDetailFactory.java && grep -q 'setType' libs/error-handling/src/main/java/com/tripplanner/errors/ProblemDetailFactory.java && grep -q 'setProperty.*code' libs/error-handling/src/main/java/com/tripplanner/errors/ProblemDetailFactory.java && test -f libs/error-handling/src/main/java/com/tripplanner/errors/ErrorCode.java && grep -q 'AUTH_UNAUTHORIZED("auth.unauthorized")' libs/error-handling/src/main/java/com/tripplanner/errors/ErrorCode.java && grep -q 'AUTH_RATE_LIMITED("auth.rate_limited")' libs/error-handling/src/main/java/com/tripplanner/errors/ErrorCode.java && [ "$(grep -cE '^\s+[A-Z_]+\(' libs/error-handling/src/main/java/com/tripplanner/errors/ErrorCode.java | head -1)" = "2" ] && ! grep -q '@ControllerAdvice' libs/error-handling/src/main/java/com/tripplanner/errors/*.java && ! grep -q 'GlobalExceptionHandler' libs/error-handling/src/main/java/com/tripplanner/errors/*.java</automated>
  </verify>
  <acceptance_criteria>
    - `ErrorCode` enum has EXACTLY 2 constants: `AUTH_UNAUTHORIZED` (`"auth.unauthorized"`) and `AUTH_RATE_LIMITED` (`"auth.rate_limited"`). Adding more violates D-05.
    - `ProblemDetailFactory` is `final`, has a private constructor, and exposes one static `of(HttpStatus, ErrorCode, String)` method.
    - No `@ControllerAdvice` / `@ExceptionHandler` / `GlobalExceptionHandler` anywhere in this lib.
    - `./gradlew :libs:error-handling:compileJava` exits 0.
  </acceptance_criteria>
  <done>libs/error-handling resolves; consumers can call `ProblemDetailFactory.of(HttpStatus.UNAUTHORIZED, ErrorCode.AUTH_UNAUTHORIZED, "JWT signature invalid")`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4.2: Create libs/api-contracts as an empty module (folder + build file + .gitkeep)</name>
  <files>libs/api-contracts/build.gradle.kts, libs/api-contracts/src/main/java/.gitkeep</files>
  <read_first>
    - .planning/phases/00-monorepo-scaffolding/00-CONTEXT.md (lines 76-77 — D-06 empty module rationale; line 80 — D-07 jwt-common does NOT exist)
    - .planning/phases/00-monorepo-scaffolding/00-PATTERNS.md (lines 378-386 — Bucket B `api-contracts/build.gradle.kts` excerpt; line 343 — `.gitkeep` rationale)
    - .planning/phases/00-monorepo-scaffolding/00-RESEARCH.md (lines 342-344 — Recommended Project Structure for `libs/api-contracts/`)
  </read_first>
  <action>
    1. **`libs/api-contracts/build.gradle.kts`** verbatim from PATTERNS.md Bucket B:
       ```kotlin
       // D-06: Empty Gradle module so Phase 1 can add UserContext later
       // without modifying settings.gradle.kts or any service's build.gradle.kts.
       plugins {
           `java-library`
       }
       // No dependencies. No source files. Phase 1 lands com.tripplanner.contracts.UserContext here.
       ```
       Note: do NOT add `spring-dependency-management` plugin — there's nothing to manage. Do NOT add any deps. The point is the module compiles to an empty jar; consumers can already `implementation(project(":libs:api-contracts"))` it without changing anything when Phase 1 adds the UserContext record.

    2. **`libs/api-contracts/src/main/java/.gitkeep`** — empty file. Forces git to track the otherwise-empty source directory so Phase 1's `UserContext.java` lands in a directory that already exists.

    3. **DO NOT** create `libs/jwt-common/`. Per D-07 / C22 that directory must NOT exist after Phase 0. If it somehow does (from a stale checkout), remove it and update `settings.gradle.kts` if it references `:libs:jwt-common` (it should not — verify Wave 1's `settings.gradle.kts` does NOT include it).
  </action>
  <verify>
    <automated>test -f libs/api-contracts/build.gradle.kts && grep -q 'java-library' libs/api-contracts/build.gradle.kts && ! grep -q 'dependencies' libs/api-contracts/build.gradle.kts && test -f libs/api-contracts/src/main/java/.gitkeep && [ ! -d libs/jwt-common ] && ! grep -q ':libs:jwt-common' settings.gradle.kts</automated>
  </verify>
  <acceptance_criteria>
    - `libs/api-contracts/build.gradle.kts` declares only the `java-library` plugin; no dependencies block.
    - `libs/api-contracts/src/main/java/.gitkeep` exists (so the otherwise-empty dir survives git).
    - `libs/jwt-common/` does NOT exist (Phase 1 owns it per D-07 / C22).
    - `settings.gradle.kts` does NOT include `:libs:jwt-common` (sanity check on Wave 1's output).
    - `./gradlew :libs:api-contracts:compileJava` exits 0 (compiles an empty jar).
  </acceptance_criteria>
  <done>Empty api-contracts module wired into the build graph; jwt-common deliberately absent (Phase 1 will add it).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application code ↔ HTTP error response | Error responses must not leak stack traces or internal paths to clients |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-00-13 | Information Disclosure | `ProblemDetail.detail` field carrying internal exception messages with stack traces or DB error info | MEDIUM | mitigate | `ProblemDetailFactory.of()` accepts `detail` from caller — caller responsibility. Phase 1 GlobalExceptionHandler will sanitize messages before passing them in. Phase 0 has no callers, so not yet exploitable. Application yml will set `server.error.include-stacktrace=never` in `docker` profile (Wave 3 service config). |
| T-00-14 | Tampering | Future contributor adds `ErrorCode` constants with paths/queries bypassing intended workflows | LOW | accept | Code review; D-05 explicitly limits Phase 0 to 2 baseline codes. |
| T-00-15 | Information Disclosure | `TYPE_BASE = "https://tripplanner.example.com/errors/"` URI leaks the example.com domain in production | LOW | accept | Portfolio scope; v1 is local-only deployment. The `type` URI is informational; consumers do not dereference it. |
</threat_model>

<verification>
- `./gradlew :libs:error-handling:compileJava` exits 0.
- `./gradlew :libs:api-contracts:compileJava` exits 0.
- `./gradlew projects` lists `:libs:observability`, `:libs:error-handling`, `:libs:api-contracts`, and the 5 services (8 subprojects total).
- `[ ! -d libs/jwt-common ]` (jwt-common does not exist).
- `grep -c 'enum ErrorCode {' libs/error-handling/**/*.java` returns 1.
- `grep -cE '^\s+(AUTH_UNAUTHORIZED|AUTH_RATE_LIMITED)' libs/error-handling/src/main/java/com/tripplanner/errors/ErrorCode.java` returns 2.
</verification>

<success_criteria>
- libs/error-handling builds clean and exposes the 2-code enum + factory.
- libs/api-contracts is an empty buildable module.
- libs/jwt-common does not exist (Phase 1 ownership preserved).
- All three libs (observability + error-handling + api-contracts) compile in parallel within Wave 2.
</success_criteria>

<output>
After completion, create `.planning/phases/00-monorepo-scaffolding/00-04-SUMMARY.md` summarizing:
- ErrorCode enum entries (must be exactly 2).
- Confirmation libs/jwt-common does NOT exist.
- Subproject count from `./gradlew projects` output.
</output>
