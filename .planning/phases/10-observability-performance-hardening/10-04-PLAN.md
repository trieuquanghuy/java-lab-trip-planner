---
phase: 10-observability-performance-hardening
plan: 04
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified:
  - README.md
  - docs/perf/k6-search-100rps-report.md
autonomous: true
requirements: [NFR-09]
must_haves:
  truths:
    - "README has architecture diagram showing service topology and observability stack"
    - "README documents how to run, test, and view traces/metrics"
    - "README mentions CI status badge and links to docs/"
  artifacts:
    - path: "README.md"
      provides: "Updated project README with architecture diagram and setup instructions"
  key_links:
    - from: "README.md"
      to: "docs/"
      via: "Links to detailed documentation"
      pattern: "docs/"
---

<objective>
Update project README with architecture diagram, setup guide, and observability documentation.

Purpose: Portfolio-ready README showing architecture, how to run, CI status, and links to perf/observability docs.
Output: Updated README.md with ASCII architecture diagram and complete setup instructions.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@README.md
@docs/02-architecture.md
@infra/docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README with architecture diagram and documentation</name>
  <files>README.md</files>
  <action>
    Rewrite README.md to be portfolio-ready:
    
    1. **Header**: Project name, one-line description, CI badge placeholder
    2. **Architecture Diagram**: ASCII/Mermaid diagram showing:
       - Frontend (React) → API Gateway → Auth/Trip/Destination services
       - Eureka for discovery
       - PostgreSQL (shared, schema-per-service)
       - Redis for caching
       - Zipkin for tracing
       - Prometheus + Grafana for metrics
    3. **Tech Stack**: Table of key technologies with versions
    4. **Quick Start**: 
       - Prerequisites (Java 21, Docker, pnpm)
       - `docker compose -f infra/docker-compose.yml up -d`
       - `./gradlew bootRun` (or individual services)
       - `cd frontend && pnpm dev`
       - URLs: frontend :5173, gateway :8180, Zipkin :9411, Grafana :3000
    5. **Testing**:
       - Backend: `./gradlew test jacocoTestReport`
       - Frontend: `cd frontend && pnpm test`
       - Load test: `k6 run scripts/k6-search-load.js`
    6. **Observability**:
       - Zipkin traces: http://localhost:9411
       - Prometheus metrics: http://localhost:9090
       - Grafana dashboards: http://localhost:3000
    7. **Project Structure**: Brief tree showing monorepo layout
    8. **Documentation**: Links to docs/ directory
    
    Keep it concise — portfolio reviewers scan quickly. Use badges, tables, and code blocks.
  </action>
  <verify>
    <automated>grep -q "Architecture" README.md && grep -q "Quick Start" README.md && grep -q "Zipkin" README.md && echo "OK"</automated>
  </verify>
  <done>README is portfolio-ready with architecture diagram, quick start, testing commands, and observability links</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Documentation-only plan, no runtime changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | I (Information Disclosure) | README.md | accept | No secrets in README; only localhost URLs and public architecture |
</threat_model>

<verification>
- README contains architecture diagram
- README contains quick start with docker compose command
- README links to docs/ and mentions observability tools
</verification>

<success_criteria>
README is complete, portfolio-ready, and documents the full system architecture and how to run/test/observe it.
</success_criteria>

<output>
After completion, create `.planning/phases/10-observability-performance-hardening/10-04-SUMMARY.md`
</output>
