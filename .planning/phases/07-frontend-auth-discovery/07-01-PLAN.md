---
phase: 07-frontend-auth-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/api.ts
  - frontend/src/types/auth.ts
  - frontend/src/components/ui/button.tsx
  - frontend/src/components/ui/input.tsx
  - frontend/src/components/ui/card.tsx
  - frontend/src/components/ui/badge.tsx
  - frontend/src/components/ui/skeleton.tsx
  - frontend/src/components/ui/form.tsx
  - frontend/src/components/ui/label.tsx
  - frontend/src/components/ui/popover.tsx
  - frontend/src/components/ui/sonner.tsx
  - frontend/src/components/Layout.tsx
  - frontend/src/App.tsx
  - frontend/src/pages/NotFoundPage.tsx
  - frontend/package.json
autonomous: true
requirements: [TRIP-05]
must_haves:
  truths:
    - "App renders with a Layout containing header, main outlet, and navigation"
    - "All routes defined and render their page components"
    - "TypeScript types mirror backend DTOs exactly"
    - "shadcn/ui primitives are available for use"
  artifacts:
    - path: "frontend/src/types/api.ts"
      provides: "Backend DTO types for search, destinations, errors"
    - path: "frontend/src/types/auth.ts"
      provides: "Auth-related types (login/signup request/response, user)"
    - path: "frontend/src/components/Layout.tsx"
      provides: "App shell with header, nav links, and Outlet"
    - path: "frontend/src/App.tsx"
      provides: "Route definitions for all Phase 7 pages"
    - path: "frontend/src/pages/NotFoundPage.tsx"
      provides: "404 catch-all page"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/Layout.tsx"
      via: "Route element wrapping all child routes"
      pattern: "<Route element={<Layout"
---

<objective>
Set up the frontend foundation: TypeScript types mirroring backend DTOs, install shadcn/ui components, create the app Layout shell with navigation, wire React Router routes for all Phase 7 pages, and add a NotFound page.

Purpose: Every subsequent plan imports from these types and uses these UI primitives. This is the contract layer.
Output: Types, UI components, routing, layout shell.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-frontend-auth-discovery/07-CONTEXT.md
@.planning/phases/07-frontend-auth-discovery/07-RESEARCH.md
@frontend/package.json
@frontend/src/main.tsx
@frontend/src/App.tsx
@frontend/components.json
@frontend/tsconfig.json
@frontend/vite.config.ts

<interfaces>
<!-- Backend DTO contracts extracted from Java source -->

From services/auth-service:
  SignupRequest: { email: string, password: string }
  SignupResponse: { userId: string (UUID) }
  LoginRequest: { email: string, password: string }
  LoginResponse: { accessToken: string, expiresIn: number, user: UserResponse }
  UserResponse: { id: string (UUID), email: string, emailVerified: boolean }
  RefreshResponse: { accessToken: string, expiresIn: number }

From services/destination-service:
  SearchResponse: { items: CitySearchItem[] }
  CitySearchItem: { type: string, name: string, country: string, lat: number, lng: number }
  NearbyResponse: { items: NearbyItem[], fromCache: boolean, providerStatus: ProviderStatus }
  NearbyItem: { providerRef: string, name: string, category: string|null, rating: number|null, photoUrl: string|null, lat: number, lng: number }
  DestinationDetailResponse: { providerRef: string, name: string, category: string|null, shortDescription: string|null, rating: number|null, lat: number, lng: number, address: string|null, website: string|null, photos: string[], openingHours: Record<string,string>|null, fromCache: boolean, fetchedAt: string }

Error shape (RFC 7807 ProblemDetail):
  ApiError: { type: string, title: string, status: number, detail: string, code: string }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies + generate shadcn/ui components</name>
  <files>frontend/package.json</files>
  <read_first>frontend/package.json, frontend/components.json</read_first>
  <action>
In the frontend directory:

1. Install new dependencies:
   ```
   pnpm add react-hook-form@^7.55.0 @hookform/resolvers@^5.0.0 zod@^3.23.0 sonner@^2.0.0
   ```

2. Generate shadcn/ui components (these use the existing components.json config):
   ```
   pnpm dlx shadcn@latest add button input card badge skeleton form label popover sonner separator --yes
   ```

This installs all UI primitives needed by Phase 7 plans. The `--yes` flag skips interactive prompts. The `components.json` already targets `@/components/ui` with Tailwind v3 + CSS variables.
  </action>
  <verify>
    <automated>cd frontend && cat package.json | grep -E "react-hook-form|@hookform/resolvers|zod|sonner" && ls src/components/ui/button.tsx src/components/ui/input.tsx src/components/ui/card.tsx</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "react-hook-form", "@hookform/resolvers", "zod", "sonner" in dependencies
    - src/components/ui/ contains button.tsx, input.tsx, card.tsx, badge.tsx, skeleton.tsx, form.tsx, label.tsx, popover.tsx, sonner.tsx, separator.tsx
    - pnpm install exits 0 with no peer dependency conflicts
  </acceptance_criteria>
  <done>All shadcn/ui components generated and new deps installed cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Create TypeScript types + Layout + routing</name>
  <files>frontend/src/types/api.ts, frontend/src/types/auth.ts, frontend/src/components/Layout.tsx, frontend/src/App.tsx, frontend/src/pages/NotFoundPage.tsx</files>
  <read_first>frontend/src/App.tsx, frontend/src/main.tsx, frontend/src/lib/utils.ts</read_first>
  <action>
1. Create `src/types/api.ts` with all backend DTO types:
   - `CitySearchItem` — { type: string; name: string; country: string; lat: number; lng: number }
   - `SearchResponse` — { items: CitySearchItem[] }
   - `NearbyItem` — { providerRef: string; name: string; category: string | null; rating: number | null; photoUrl: string | null; lat: number; lng: number }
   - `ProviderStatus` — { openTripMap: string; foursquare: string }
   - `NearbyResponse` — { items: NearbyItem[]; fromCache: boolean; providerStatus: ProviderStatus }
   - `DestinationDetailResponse` — { providerRef: string; name: string; category: string | null; shortDescription: string | null; rating: number | null; lat: number; lng: number; address: string | null; website: string | null; photos: string[]; openingHours: Record<string, string> | null; fromCache: boolean; fetchedAt: string }
   - `ApiError` — { type: string; title: string; status: number; detail: string; code: string }

2. Create `src/types/auth.ts`:
   - `LoginRequest` — { email: string; password: string }
   - `LoginResponse` — { accessToken: string; expiresIn: number; user: UserInfo }
   - `SignupRequest` — { email: string; password: string }
   - `SignupResponse` — { userId: string }
   - `RefreshResponse` — { accessToken: string; expiresIn: number }
   - `UserInfo` — { id: string; email: string; emailVerified: boolean }

3. Create `src/components/Layout.tsx`:
   - Import `Outlet`, `Link`, `useLocation` from react-router-dom
   - Header with:
     - Left: "TripPlanner" brand text linking to `/`
     - Right nav: "My Trips" link (to `/trips`), "Favorites" heart icon link (to `/favorites`), conditional Login/Signup buttons OR user email + Logout button
   - `<Outlet />` as main content area
   - Footer: minimal "© 2026 TripPlanner" text
   - Use Tailwind classes: `min-h-screen flex flex-col`, header `border-b border-border px-4 h-14 flex items-center justify-between`
   - Import `useAuthStore` from `@/features/auth/auth.store` (will be created in Plan 02 — use a placeholder import that TypeScript allows with a `// @ts-expect-error` comment or define a temporary inline type)
   - Actually: since Plan 01 and Plan 02 are Wave 1 (parallel), Layout should NOT import auth store yet. Instead, render static nav with Login/Signup links always visible. Plan 03 (Wave 2) will update Layout to be auth-aware.

4. Replace `src/App.tsx` content with React Router routes:
   ```tsx
   import { Routes, Route } from 'react-router-dom';
   import { Layout } from '@/components/Layout';
   import { NotFoundPage } from '@/pages/NotFoundPage';

   // Lazy-loaded pages will be added in subsequent plans
   // For now, render placeholder divs for pages not yet created
   function PlaceholderPage({ name }: { name: string }) {
     return <div className="py-8"><h1 className="text-xl font-semibold">{name}</h1></div>;
   }

   export default function App() {
     return (
       <Routes>
         <Route element={<Layout />}>
           <Route path="/" element={<PlaceholderPage name="Home" />} />
           <Route path="/destinations/:providerRef" element={<PlaceholderPage name="Destination Detail" />} />
           <Route path="/login" element={<PlaceholderPage name="Login" />} />
           <Route path="/signup" element={<PlaceholderPage name="Sign Up" />} />
           <Route path="/verify" element={<PlaceholderPage name="Verify Email" />} />
           <Route path="*" element={<NotFoundPage />} />
         </Route>
       </Routes>
     );
   }
   ```

5. Create `src/pages/NotFoundPage.tsx`:
   - Display "404 — Page not found" centered with a "Go Home" link button
   - Use shadcn Button component with `asChild` wrapping a `<Link to="/">`
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` exits 0 (no type errors)
    - src/types/api.ts exports CitySearchItem, SearchResponse, NearbyItem, NearbyResponse, DestinationDetailResponse, ApiError
    - src/types/auth.ts exports LoginRequest, LoginResponse, SignupRequest, SignupResponse, RefreshResponse, UserInfo
    - src/components/Layout.tsx contains `<Outlet />` and nav links to `/`, `/login`, `/signup`
    - src/App.tsx contains `<Routes>` with Route for `/`, `/destinations/:providerRef`, `/login`, `/signup`, `/verify`, `*`
    - src/pages/NotFoundPage.tsx contains "404" text and a Link to "/"
  </acceptance_criteria>
  <done>TypeScript compiles cleanly; all types defined; Layout renders with nav; routes mapped to pages</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API Gateway | All user input crosses here (search queries, form data) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | S (Spoofing) | types/auth.ts | accept | Types are compile-time only; runtime auth handled in Plan 02 interceptor |
| T-07-02 | T (Tampering) | Layout nav links | accept | Nav links are static; no sensitive data displayed in this plan |
</threat_model>

<verification>
- `pnpm tsc --noEmit` passes
- `pnpm test` passes (existing App.test.tsx may need updating — update test to render route)
- Dev server renders Layout with nav at localhost:5173
</verification>

<success_criteria>
All shadcn/ui components generated, TypeScript types match backend DTOs exactly, routing structure renders Layout shell with navigation and Outlet.
</success_criteria>

<output>
After completion, create `.planning/phases/07-frontend-auth-discovery/07-01-SUMMARY.md`
</output>
