# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: auth.spec.ts >> Authentication >> signup shows validation error for invalid email
- Location: e2e/auth.spec.ts:28:3

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByText(/valid email|invalid email/i)
Expected: visible
Timeout: 3000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 3000ms
  - waiting for getByText(/valid email|invalid email/i)

```

```yaml
- status: Navigated to Trip Planner
- link "Skip to main content":
  - /url: "#main-content"
- banner:
  - link "TripPlanner":
    - /url: /
  - navigation:
    - button "Switch to dark mode":
      - img
    - link "Login":
      - /url: /login
    - link "Sign Up":
      - /url: /signup
- main:
  - text: Sign Up Email
  - textbox "Email":
    - /placeholder: you@example.com
    - text: not-an-email
  - text: Password
  - textbox "Password":
    - /placeholder: ••••••••
    - text: password123
  - button "Create Account"
  - paragraph:
    - text: Already have an account?
    - link "Log in":
      - /url: /login
- contentinfo: © 2026 TripPlanner
- region "Notifications alt+T"
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test.describe('Authentication', () => {
  4  |   test('signup page renders correctly', async ({ page }) => {
  5  |     await page.goto('/signup');
  6  |     await expect(page.getByRole('heading', { name: /sign up|create account/i })).toBeVisible();
  7  |     await expect(page.getByLabel(/email/i)).toBeVisible();
  8  |     await expect(page.getByLabel(/password/i)).toBeVisible();
  9  |     await expect(page.getByRole('button', { name: /sign up|create account/i })).toBeVisible();
  10 |   });
  11 | 
  12 |   test('login page renders correctly', async ({ page }) => {
  13 |     await page.goto('/login');
  14 |     await expect(page.getByRole('heading', { name: /log in|sign in/i })).toBeVisible();
  15 |     await expect(page.getByLabel(/email/i)).toBeVisible();
  16 |     await expect(page.getByLabel(/password/i)).toBeVisible();
  17 |     await expect(page.getByRole('button', { name: /log in|sign in/i })).toBeVisible();
  18 |   });
  19 | 
  20 |   test('login shows error on invalid credentials', async ({ page }) => {
  21 |     await page.goto('/login');
  22 |     await page.getByLabel(/email/i).fill('nonexistent@example.com');
  23 |     await page.getByLabel(/password/i).fill('wrongpassword');
  24 |     await page.getByRole('button', { name: /log in|sign in/i }).click();
  25 |     await expect(page.getByText(/invalid|incorrect|wrong|failed/i)).toBeVisible({ timeout: 5000 });
  26 |   });
  27 | 
  28 |   test('signup shows validation error for invalid email', async ({ page }) => {
  29 |     await page.goto('/signup');
  30 |     await page.getByLabel(/email/i).fill('not-an-email');
  31 |     await page.getByLabel(/password/i).fill('password123');
  32 |     await page.getByRole('button', { name: /sign up|create account/i }).click();
> 33 |     await expect(page.getByText(/valid email|invalid email/i)).toBeVisible({ timeout: 3000 });
     |                                                                ^ Error: expect(locator).toBeVisible() failed
  34 |   });
  35 | 
  36 |   test('unauthenticated user is redirected from /trips to /login', async ({ page }) => {
  37 |     await page.goto('/trips');
  38 |     await expect(page).toHaveURL(/login/, { timeout: 5000 });
  39 |   });
  40 | 
  41 |   test('unauthenticated user is redirected from /favorites to /login', async ({ page }) => {
  42 |     await page.goto('/favorites');
  43 |     await expect(page).toHaveURL(/login/, { timeout: 5000 });
  44 |   });
  45 | });
  46 | 
```