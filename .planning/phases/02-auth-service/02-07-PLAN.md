---
phase: 02-auth-service
plan: 07
type: execute
wave: 6
depends_on: ["02-01", "02-02", "02-03", "02-04", "02-05", "02-06"]
files_modified:
  - scripts/smoke.sh
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, NFR-05]
must_haves:
  truths:
    - "scripts/smoke.sh adds 5 Phase 2 success-criteria checks (signup→verify-link-in-mailhog→login E2E; unverified-cannot-login opaque error; /me-substitute /logout flow; refresh-rotation + post-logout-revocation; rate-limit 6th attempt 429)"
    - "User runs `docker compose down -v && docker compose up -d --wait && bash scripts/smoke.sh` end-to-end on a fresh checkout and observes exit 0"
    - "User opens MailHog UI at http://localhost:8025 mid-flow and visually confirms the verification email rendering — subject 'Verify your Trip Planner account', body verbatim per UI-SPEC §Email Copy Contract"
    - "Phase 2 declared complete only after the user signals 'approved' on the human-verify checkpoint"
  artifacts:
    - path: "scripts/smoke.sh"
      provides: "Phase 2 success-criteria gate added to existing Phase 0/1 smoke checks"
      contains: "auth"
  key_links:
    - from: "scripts/smoke.sh"
      to: "ROADMAP Phase 2 SC#1-#5"
      via: "5 new --criterion checks targeting /api/auth/* end-to-end via gateway"
      pattern: "/api/auth/(signup|verify|login|refresh|logout)"
---

<objective>
Final phase-gate. Two tasks:

1. Extend `scripts/smoke.sh` with 5 new criterion checks mapping to ROADMAP Phase 2 SC#1-#5. Each check is a curl-against-the-running-stack assertion.
2. Run the full smoke + visual MailHog check, then a human-verify checkpoint to declare Phase 2 done.

The smoke script is the operational counterpart to Plan 06's automated test suite — it confirms the WIRED system (gateway -> auth-service -> Postgres -> Redis -> Mailhog) works end-to-end against compose. Plan 06's ITs use Testcontainers + GreenMail; this plan exercises the actual `infra/docker-compose.yml` stack.

The human-verify step is the only place Plan 07 deviates from automation — visual rendering of the verification email in MailHog UI is the one assertion no automated check can faithfully replace (UI-SPEC §Email Copy Contract has visual elements: subject formatting, line breaks, em-dash rendering — all faithfully shown only by a real mail client).

Output: Phase 2 declared COMPLETE in STATE.md (orchestrator's responsibility after this plan's checkpoint signals approval).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-auth-service/02-CONTEXT.md
@.planning/phases/02-auth-service/02-UI-SPEC.md
@.planning/phases/02-auth-service/02-VALIDATION.md
@.planning/phases/02-auth-service/02-06-SUMMARY.md
@scripts/README.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 7.1: Extend scripts/smoke.sh with 5 Phase 2 success-criteria checks</name>
  <files>scripts/smoke.sh</files>
  <read_first>
    - scripts/smoke.sh — read full file (~200 lines from Phase 0 plan 00-02 + Phase 1 additions). Find the existing `--criterion` switch structure; new checks slot into the same dispatch.
    - scripts/README.md — read documentation conventions (each --criterion gets a docstring; failures print exit code + reason).
    - .planning/ROADMAP.md "Phase 2: Auth Service" Success Criteria — 5 numbered criteria are the verbatim source.
    - infra/docker-compose.yml — confirm gateway:8080 + mailhog UI:8025 + auth-service:8081 are bound (loopback) for the smoke script's curl targets.
    - .planning/phases/02-auth-service/02-UI-SPEC.md §Server-Driven Copy Contract — assert verbatim detail strings.
  </read_first>
  <action>
Modify `scripts/smoke.sh` to ADD 5 new criterion checks. Each criterion is invoked via `bash scripts/smoke.sh --criterion auth-N` (where N is 1-5), and the unqualified `bash scripts/smoke.sh` runs ALL criteria including the existing Phase 0/1 checks plus these 5 new ones.

Add 5 functions (or 5 case branches if the existing script uses a `case "$1"` dispatch — match the existing convention by reading the file first):

**auth-1: signup -> verify-via-mailhog-API -> login E2E (ROADMAP SC#1)**
- POST `/api/auth/signup` with `{email: smoke-1@test.local, password: smokepassword}` -> assert HTTP 201 + body has `userId`.
- Wait 2 seconds for async send to land in MailHog.
- GET MailHog API at `http://localhost:8025/api/v2/messages` -> JSON parse to find the most recent message addressed to `smoke-1@test.local`. (jq is in the script's Phase 0 toolchain check; use `jq -r '.items[0].Content.Body'` to extract the body.)
- Extract the verification token (the 64-hex string after `token=` in the body).
- GET `/api/auth/verify?token=<extracted>` -> assert HTTP 302 + Location header ends with `/verify?status=success`.
- POST `/api/auth/login` with same credentials -> assert HTTP 200 + body has `accessToken` + Set-Cookie header contains `refresh_token=`.
- Print `[OK] auth-1: signup -> verify -> login`.

**auth-2: unverified account cannot log in; opaque error message (ROADMAP SC#2)**
- POST `/api/auth/signup` with `{email: smoke-2@test.local, password: smokepassword}` -> assert HTTP 201.
- (Skip the verify step — keep account unverified.)
- POST `/api/auth/login` -> assert HTTP 403 + body `$.code == "auth.email_not_verified"` + body `$.detail == "Please verify your email before logging in."`.
- Verify the error message does NOT reveal whether the account exists by ALSO testing a non-existent account: POST `/api/auth/login` with `{email: never@registered.local, password: x}` -> assert HTTP 400 + body `$.code == "auth.invalid_credentials"`. Both errors are generic; an attacker cannot distinguish "doesn't exist" from "exists but unverified".
- Print `[OK] auth-2: unverified-cannot-login + opaque`.

**auth-3: logged-in user can call authenticated route, then logout invalidates session (ROADMAP SC#3)**
- (Reuse smoke-1's user — already verified. Re-login to capture a fresh access JWT + refresh cookie.)
- (Open Q1 RESOLVED: NO /me endpoint. Substitute by hitting `/api/auth/logout` with the JWT — that's an authenticated route. Logout itself returns 204 + clears cookie.)
- POST `/api/auth/logout` with the access JWT (Bearer header) AND the refresh cookie -> assert HTTP 204 + Set-Cookie header has `Max-Age=0`.
- Now retry the SAME refresh cookie at `/api/auth/refresh` -> assert HTTP 401 + body `$.code == "auth.refresh_invalid"`.
- Print `[OK] auth-3: authenticated route + logout invalidation`.

**auth-4: refresh rotation + post-logout revocation (ROADMAP SC#4)**
- (Re-signup + verify + login a new user smoke-4@test.local — full setup.)
- POST `/api/auth/refresh` with the cookie -> assert HTTP 200 + body has `accessToken` + new `refresh_token` cookie value (DIFFERENT from the original).
- POST `/api/auth/logout` with the NEW cookie -> 204.
- POST `/api/auth/refresh` with the NEW cookie (now revoked) -> 401 `auth.refresh_invalid`.
- Print `[OK] auth-4: refresh rotation + post-logout revocation`.

**auth-5: rate-limit at 6th attempt (NFR-05 / docs/05 §10 #8 IP+email leg)**
- (Use a fresh email to avoid Redis state from prior runs: smoke-5-$(date +%s)@test.local.)
- 5 wrong-password attempts -> assert each returns 400 `auth.invalid_credentials`.
- 6th attempt -> assert 429 + `$.code == "auth.rate_limited"` + `$.detail == "Too many attempts. Please try again later."`.
- Print `[OK] auth-5: rate-limit 6th attempt`.

Also add at the END of the smoke script a summary line if all 5 auth criteria pass: `[OK] PHASE 2 SMOKE: 5/5 auth criteria passed`.

Add documentation to `scripts/README.md` (or as comments in the script header) listing the 5 new `--criterion auth-N` options.

Implementation note: jq must be available. Phase 0 plan 00-02 already documented the jq-detection-with-grep-fallback pattern; reuse it for the MailHog body parsing. If the host lacks jq, fail with a friendly message: `"jq is required for auth-1 mailhog parsing; install jq or run inside CI"`.
  </action>
  <verify>
    <automated>cd /Users/huyqtrieu/Desktop/Practice/java-lab && bash -n scripts/smoke.sh && grep -q "auth-1" scripts/smoke.sh && grep -q "auth-2" scripts/smoke.sh && grep -q "auth-3" scripts/smoke.sh && grep -q "auth-4" scripts/smoke.sh && grep -q "auth-5" scripts/smoke.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/smoke.sh` syntactically valid (`bash -n scripts/smoke.sh` exits 0)
    - File contains 5 distinct criterion identifiers: `auth-1`, `auth-2`, `auth-3`, `auth-4`, `auth-5` (each appears at least once)
    - File contains literal `/api/auth/signup`
    - File contains literal `/api/auth/verify`
    - File contains literal `/api/auth/login`
    - File contains literal `/api/auth/refresh`
    - File contains literal `/api/auth/logout`
    - File contains literal `localhost:8025` (MailHog UI/API endpoint for auth-1 token extraction)
    - File contains literal `auth.email_not_verified`
    - File contains literal `auth.invalid_credentials`
    - File contains literal `auth.refresh_invalid`
    - File contains literal `auth.rate_limited`
    - File contains literal `Please verify your email before logging in.` (UI-SPEC verbatim)
    - File contains literal `Too many attempts. Please try again later.` (UI-SPEC verbatim)
    - File contains JQ-availability detection with grep fallback (matches Phase 0 pattern)
    - `bash -n scripts/smoke.sh` exits 0
  </acceptance_criteria>
  <done>scripts/smoke.sh ships 5 new --criterion auth-N checks mapping 1:1 to ROADMAP Phase 2 SC#1-#5. Each curl-against-running-stack pattern reuses the existing Phase 0/1 dispatch convention. JQ availability with grep fallback honored.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7.2: Final Phase 2 phase-gate — fresh-checkout smoke + MailHog visual verification</name>
  <what-built>
- 5 Flyway migrations (V2 users / V3 email_verification_tokens / V4 refresh_tokens) + 3 JPA entities + 3 repositories
- JwtIssuer in libs/jwt-common (HS256 15-min, sibling-twin to JwtVerifier)
- 5 endpoints (signup / verify-redirect / login / refresh / logout) with verbatim UI-SPEC error responses
- @Async email send via Mailhog (D-01) with body verbatim per UI-SPEC §Email Copy Contract
- Refresh-token rotation with REPEATABLE_READ + SELECT FOR UPDATE (D-13) and BOTH-DIRECTIONS chain revocation on replay (D-10 / Pitfall 4)
- Login rate limiter — Redis Lua INCR+EXPIRE atomic, key `rl:login:fail:{ip}:{email_lower}` (D-06)
- Daily @Scheduled token cleanup at 02:00 UTC (D-17)
- 4 mandatory @Tag("security") integration tests (CONTEXT D-15 #5/#6/#7/#8 — DeletedRefresh / RotatedRefresh / EmailNotVerified / LoginRateLimit)
- Happy-path E2E IT covering ROADMAP SC#1/#3/#4
- AuthControllerAdviceIT BL-01 contract — $.code at root, $.properties.code.doesNotExist()
- BL-01 negative-assertion regression gate added to 4 gateway IT files
- 2 unit tests (RefreshTokenServiceTest BOTH-DIRECTIONS Pitfall 4 mitigation; LoginRateLimiterTest Lua atomicity)
- scripts/smoke.sh extended with 5 new auth-N criterion checks
  </what-built>
  <how-to-verify>
1. **Tear down + rebuild fresh:**
   ```
   docker compose down -v
   docker compose up -d --wait
   ```
   Wait until `docker compose ps` shows ALL services Healthy (auth-service should now boot Flyway V2/V3/V4 against the fresh Postgres). Eureka dashboard at `http://localhost:8761` should list `AUTH-SERVICE` (1 instance) alongside the other 4 services.

2. **Run full smoke (Phase 0 + 1 + 2 criteria):**
   ```
   bash scripts/smoke.sh
   ```
   Expected: ALL criteria PASS, including the 5 new `auth-1` through `auth-5` lines printing `[OK] auth-N: ...`. Final line: `[OK] PHASE 2 SMOKE: 5/5 auth criteria passed`.

3. **MailHog UI visual check:**
   - Open `http://localhost:8025` in a browser.
   - Confirm at least one inbox entry from `Trip Planner <no-reply@tripplanner.local>` (auth-1 / auth-2 / auth-4 / auth-5 setup all triggered signups).
   - Click the most recent message and visually verify:
     - **Subject:** `Verify your Trip Planner account` (exact)
     - **Body** (plain-text rendering matches UI-SPEC §Email Copy Contract verbatim):
       ```
       Welcome to Trip Planner!

       Click the link below to verify your email and activate your account:

       http://localhost:8080/api/auth/verify?token=<64-hex>

       This link expires in 24 hours.

       If you didn't create an account, you can safely ignore this email.

       — The Trip Planner team
       ```
     - The em-dash before "The Trip Planner team" should render as `—` (U+2014), NOT `--` or `-`.
     - The verification URL should be on its own line.
     - No HTML rendering (the message body is plain text).

4. **Run the @Tag("security") suite directly:**
   ```
   ./gradlew :services:auth-service:test -PincludeTags=security
   ```
   Expected: 4 tests PASSED (DeletedRefreshTokenCannotBeUsedIT, RotatedRefreshTokenCannotBeReusedIT, EmailNotVerifiedCannotLoginIT, LoginRateLimitFailedAttemptsTriggersIT). Exit 0.

5. **Spot-check the gateway -> auth-service routing remains correct:**
   ```
   curl -i http://localhost:8080/api/auth/anything-not-a-real-endpoint
   ```
   Expected: 404 (or 405) — confirms the gateway still routes `/api/auth/**` to auth-service (Phase 1 D-02 routing intact).

6. **Verify NO regression in the existing Phase 0/1 smoke criteria:**
   The Phase 0 SC#1-#5 + NFR-04 + Phase 1 bypass/routing/rate-limit checks should ALL still PASS in the same `bash scripts/smoke.sh` invocation.
  </how-to-verify>
  <resume-signal>
Type one of:
- `approved` — All checks green, MailHog rendering matches UI-SPEC verbatim. Phase 2 is COMPLETE.
- `<describe issue>` — Smoke step N failed / MailHog body doesn't match UI-SPEC at line X / @Tag("security") test Y is flaky. Will be triaged before declaring done.
  </resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test smoke -> running compose stack | Smoke script curl-tests against `localhost:8080` (gateway) — exercises the production-like flow including reactive gateway, servlet auth-service, real Postgres, real Redis, real Mailhog. |
| MailHog UI visual check -> human eye | The em-dash rendering, body line breaks, and overall plain-text faithfulness are best-checked visually. No automated equivalent in v1. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-07-01 | Tampering | Visual UI-SPEC compliance | mitigate | Human-verify checkpoint catches visual drift (em-dash, line breaks, encoding) that automated assertions could miss. v2 may add a snapshot-test against captured GreenMail body bytes. |
| T-2-07-02 | Information Disclosure | smoke.sh leaks credentials | accept | smoke.sh uses fixed test-only emails (smoke-1@test.local etc.) and the `smokepassword` literal. None reach production. Documented in scripts/README.md. |
| T-2-07-03 | Repudiation | Phase-gate signoff | accept | The user's `approved` signal is captured in the SUMMARY but is informal — no signed cryptographic attestation in v1. CI green is the formal merge gate. |

</threat_model>

<verification>
After both tasks:
- `bash -n scripts/smoke.sh` exits 0 (syntactic check of the 5 new criterion branches)
- The user signals `approved` on the human-verify checkpoint AFTER running the full fresh-stack smoke
- All 5 ROADMAP Phase 2 success criteria green in smoke output
- MailHog UI visually confirms verbatim UI-SPEC email body
- @Tag("security") suite GREEN (4/4 tests)
</verification>

<success_criteria>
- scripts/smoke.sh extended with 5 ROADMAP-mapped criterion checks
- Fresh-stack `docker compose down -v && up -d --wait && bash scripts/smoke.sh` exits 0
- `./gradlew :services:auth-service:test -PincludeTags=security` exits 0 with 4 tests
- MailHog UI visual check matches UI-SPEC §Email Copy Contract verbatim (subject, body, em-dash)
- User approves the phase-gate checkpoint
- Phase 2 declared COMPLETE in STATE.md (orchestrator updates after this plan signals approval)
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-service/02-07-SUMMARY.md` capturing:
- Final smoke result (5/5 PASS or any fixes applied)
- MailHog visual confirmation date + browser used
- Any em-dash encoding gotcha (e.g., terminal vs MailHog rendering differences)
- Phase 2 final status: COMPLETE
- Cross-cutting handoff notes for Phase 3 (Destination Service — Search): which auth artifacts are now stable contracts (JWT shape, RFC 7807 envelope, /api/auth routes)
</output>
