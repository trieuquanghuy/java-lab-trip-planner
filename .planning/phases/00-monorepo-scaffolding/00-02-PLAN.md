---
phase: 00-monorepo-scaffolding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/smoke.sh
  - scripts/README.md
autonomous: true
requirements: [NFR-04]

must_haves:
  truths:
    - "`scripts/smoke.sh` is the canonical phase-gate verifier; exits non-zero on any criterion failure with a printed criterion id (D-33)."
    - "Smoke script enforces all 5 ROADMAP success criteria: compose healthy <60s, 4 services in Eureka, gateway health UP, frontend reachable, per-service Flyway history tables present."
    - "Smoke script lands in Wave 1 (NOT a final wave) so each subsequent wave's containers can be smoke-tested incrementally as they come online (D-33)."
  artifacts:
    - path: "scripts/smoke.sh"
      provides: "Executable phase-gate verifier wrapping `docker compose up -d --wait` and asserting all 5 ROADMAP SCs"
      contains: "set -euo pipefail"
      min_lines: 40
    - path: "scripts/README.md"
      provides: "Documentation of how to run smoke.sh, what each criterion means, and what failure modes look like"
  key_links:
    - from: "scripts/smoke.sh"
      to: "infra/docker-compose.yml (Wave 4)"
      via: "`docker compose ps` / `docker compose exec postgres psql` queries"
      pattern: "docker compose (ps|exec)"
    - from: "scripts/smoke.sh"
      to: "Eureka dashboard at localhost:8761"
      via: "curl GET /eureka/apps with Accept: application/json"
      pattern: "localhost:8761/eureka"
    - from: ".github/workflows/backend.yml + frontend.yml (Wave 6)"
      to: "scripts/smoke.sh"
      via: "potential CI step (added in Wave 6 plan 13)"
      pattern: "scripts/smoke.sh"
---

<objective>
Land the canonical phase-gate verifier `scripts/smoke.sh` and its README in Wave 1 per D-33. By placing smoke FIRST (not last), every subsequent wave can validate its contribution incrementally — Wave 2 lib changes don't break smoke, Wave 3 service skeletons must pass smoke at health-check level before merge, etc.

Purpose: D-33 explicitly requires this script in Wave 1. The script doubles as the verification harness for `/gsd-verify-phase` and provides the executor a one-line "did I break Phase 0?" check after every change.

Output: Executable `scripts/smoke.sh` that exits 0 when all 5 ROADMAP success criteria pass and a `scripts/README.md` documenting how to read its output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/00-monorepo-scaffolding/00-CONTEXT.md
@.planning/phases/00-monorepo-scaffolding/00-RESEARCH.md
@.planning/phases/00-monorepo-scaffolding/00-PATTERNS.md
@.planning/phases/00-monorepo-scaffolding/00-VALIDATION.md
@.planning/phases/00-monorepo-scaffolding/00-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 2.1: Author scripts/smoke.sh implementing all 5 ROADMAP success criteria</name>
  <files>scripts/smoke.sh</files>
  <read_first>
    - .planning/phases/00-monorepo-scaffolding/00-CONTEXT.md (lines 175-183 — D-33 — smoke as Wave 1 deliverable; what 5 criteria must be enforced)
    - .planning/phases/00-monorepo-scaffolding/00-PATTERNS.md (lines 901-950 — Bucket I — full smoke script excerpt)
    - .planning/phases/00-monorepo-scaffolding/00-RESEARCH.md (lines 1283-1336 — Validation Architecture; Sampling Rate; smoke script outline)
    - .planning/ROADMAP.md (lines 30-39 — Phase 0 Success Criteria #1-#5 verbatim — these MUST be the assertions)
  </read_first>
  <action>
    Create `scripts/smoke.sh` with `#!/usr/bin/env bash` shebang. Use `set -euo pipefail` at the top. Implement the script along the following structure (paste-and-edit from PATTERNS.md Bucket I and RESEARCH.md Validation Architecture, but ensure ALL 5 ROADMAP success criteria are covered):

    1. **Helper functions** at top:
       - `fail() { echo "[smoke] FAIL: $1" >&2; exit 1; }`
       - `pass() { echo "[smoke] OK:   $1"; }`

    2. **Configuration** (env vars with defaults):
       - `COMPOSE_FILE="${COMPOSE_FILE:-infra/docker-compose.yml}"`
       - `POSTGRES_DB="${POSTGRES_DB:-tripplanner}"`
       - `POSTGRES_USER="${POSTGRES_USER:-postgres}"`
       - `WAIT_TIMEOUT="${WAIT_TIMEOUT:-60}"` (seconds)

    3. **Optional preflight** — if invoked with `--up` flag (`if [[ "${1:-}" == "--up" ]]`), run `docker compose -f "$COMPOSE_FILE" up -d --wait --wait-timeout "$WAIT_TIMEOUT"` and time the wall-clock; assert <60s. Otherwise assume the user already has the stack running.

    4. **SC #1 — compose healthy** (after up returns or independent check): `docker compose -f "$COMPOSE_FILE" ps --format json | jq -e 'all(.[]; .State == "running" and (.Health == "healthy" or .Health == ""))'` (`""` covers services without a healthcheck like frontend/zipkin). Fail with id `SC#1` if any service is not running or healthy.

    5. **SC #2 — 4 services in Eureka**: `curl -sf -H "Accept: application/json" http://localhost:8761/eureka/apps` returns JSON; for each of `AUTH-SERVICE`, `TRIP-SERVICE`, `DESTINATION-SERVICE`, `API-GATEWAY`, grep the response (or use `jq '.applications.application[].name'`) and fail with id `SC#2` listing the missing service if absent. Loop with a max-30s retry+1s sleep window because Eureka registration may complete shortly after compose health.

    6. **SC #3 — gateway /actuator/health UP**: `curl -sf http://localhost:8080/actuator/health | jq -e '.status == "UP"'`. Fail with id `SC#3`.

    7. **SC #3 extension — `/__health/<svc>` end-to-end through gateway**: For `svc in auth trip destination`: `curl -sf "http://localhost:8080/__health/$svc" | jq -e '.status == "UP" and .phase == 0'`. Fail with id `SC#3-route` listing the failing service. (Note: the `phase: 0` field comes from the `/__health` placeholder controller per D-01 — cf. RESEARCH.md lines 1011-1033.)

    8. **SC #4 — frontend reachable**: `curl -sf -o /tmp/tripplanner_index.html http://localhost:5173 && test -s /tmp/tripplanner_index.html`. Fail with id `SC#4`. (Browser console-error check is manual per VALIDATION.md "Manual-Only Verifications"; smoke only asserts HTTP reachability + non-empty body.)

    9. **SC #5 — per-service Flyway history tables present**: For each `(schema, table)` pair in `("auth", "auth_flyway_schema_history")`, `("trip", "trip_flyway_schema_history")`, `("destination", "destination_flyway_schema_history")`:
       - `EXISTS=$(docker compose -f "$COMPOSE_FILE" exec -T postgres psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -tAc "SELECT 1 FROM information_schema.tables WHERE table_schema='$schema' AND table_name='$table'")`
       - Fail with id `SC#5` listing `$schema.$table` if not `1`.

    10. **Final pass message**: `echo "[smoke] ALL PASS — Phase 0 ROADMAP success criteria #1-#5 met"`. Exit 0.

    Make the script executable: `chmod +x scripts/smoke.sh`.

    **Robustness requirements:**
    - The script MUST NOT call `exit 1` from inside a subshell context that swallows the non-zero status. Use the `fail()` helper which writes to stderr and exits.
    - Each criterion check is gated by a label (`SC#1`, `SC#2`, etc.) so a failure tells the developer exactly which ROADMAP criterion broke.
    - If `jq` is missing, fall back to `grep` for SC#2 / SC#3 / SC#4 (the PATTERNS.md excerpt shows the grep variants); print a one-line note `[smoke] note: jq not found, using grep fallback`.
    - Allow running stand-alone (`./scripts/smoke.sh`) or as a preflight (`./scripts/smoke.sh --up`).
  </action>
  <verify>
    <automated>test -x scripts/smoke.sh && head -1 scripts/smoke.sh | grep -q '^#!/usr/bin/env bash' && grep -q 'set -euo pipefail' scripts/smoke.sh && grep -q 'SC#1' scripts/smoke.sh && grep -q 'SC#2' scripts/smoke.sh && grep -q 'SC#3' scripts/smoke.sh && grep -q 'SC#4' scripts/smoke.sh && grep -q 'SC#5' scripts/smoke.sh && grep -q 'localhost:8761/eureka/apps' scripts/smoke.sh && grep -q 'localhost:8080/actuator/health' scripts/smoke.sh && grep -q 'localhost:8080/__health' scripts/smoke.sh && grep -q 'localhost:5173' scripts/smoke.sh && grep -q 'flyway_schema_history' scripts/smoke.sh && grep -q 'AUTH-SERVICE\|auth-service' scripts/smoke.sh && bash -n scripts/smoke.sh</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `scripts/smoke.sh`, mode includes `+x`.
    - Bash syntax valid (`bash -n scripts/smoke.sh` exits 0).
    - All 5 SC labels present in the source: `SC#1`, `SC#2`, `SC#3`, `SC#4`, `SC#5`.
    - Hits all 5 endpoints/queries: `localhost:8761/eureka/apps`, `localhost:8080/actuator/health`, `localhost:8080/__health/<svc>`, `localhost:5173`, postgres `flyway_schema_history` table query.
    - Uses `set -euo pipefail`.
    - Has a `fail()` helper that writes to stderr and exits non-zero.
    - When the stack is NOT running, the script fails fast on SC#1 (does not hang for `WAIT_TIMEOUT` seconds unless `--up` flag is passed).
  </acceptance_criteria>
  <done>Smoke script exists, is executable, has correct bash syntax, and asserts all 5 ROADMAP success criteria with labelled failure messages.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2.2: Document smoke.sh usage in scripts/README.md</name>
  <files>scripts/README.md</files>
  <read_first>
    - .planning/phases/00-monorepo-scaffolding/00-CONTEXT.md (lines 175-183 — D-33; lines 21-22 — phase boundary "all 6 ROADMAP success criteria")
    - .planning/phases/00-monorepo-scaffolding/00-VALIDATION.md (entire file — Wave 0 reqs, sampling rate, manual verifications)
    - .planning/ROADMAP.md (lines 30-39 — Phase 0 Success Criteria — copy verbatim into the README)
    - scripts/smoke.sh (the file you just authored — read it to ensure README docs match)
  </read_first>
  <action>
    Create `scripts/README.md` with the following sections:

    1. **Title:** "Trip Planner — Scripts"
    2. **Overview**: 2 sentences. "This directory contains operational scripts. The primary one is `smoke.sh` — the Phase 0 phase-gate verifier."
    3. **`smoke.sh` — Phase 0 Verifier**:
       - Purpose paragraph: "Asserts all 5 ROADMAP Phase 0 success criteria pass against a running compose stack. Exits 0 on success; non-zero with a labelled failure message (SC#1..SC#5) on any failure."
       - Usage:
         ```
         # Standalone (assumes stack already running)
         ./scripts/smoke.sh

         # With preflight (brings the stack up via --wait, then verifies)
         ./scripts/smoke.sh --up
         ```
       - Environment overrides table: `COMPOSE_FILE` (default `infra/docker-compose.yml`), `POSTGRES_DB` (default `tripplanner`), `POSTGRES_USER` (default `postgres`), `WAIT_TIMEOUT` (default `60`).
       - **5 success criteria** table — copy verbatim from `.planning/ROADMAP.md` Phase 0 Success Criteria #1-#5 with the SC label the script emits:
         | Criterion | What it asserts |
         |-----------|-----------------|
         | SC#1 | `docker compose up` brings every container to healthy status in under 60 seconds |
         | SC#2 | All 4 services (api-gateway, auth-service, trip-service, destination-service) registered in Eureka |
         | SC#3 | `curl localhost:8080/actuator/health` returns `{"status":"UP"}` through the gateway |
         | SC#3-route | `/__health/{auth,trip,destination}` route through the gateway and return `{status: UP, phase: 0}` (D-02 validation) |
         | SC#4 | `localhost:5173` reachable (frontend dev server up) — note: console-error check is manual |
         | SC#5 | Per-service `*_flyway_schema_history` tables exist in their respective schemas |
       - **Failure modes** subsection — for each SC, a one-line "if this fails, look at:" pointer:
         - SC#1 fails → check `docker compose ps`, look for non-`healthy` services
         - SC#2 fails → likely Eureka cold-start; wait 30s and re-run; if persistent, check service logs for registration errors
         - SC#3 fails → gateway misconfigured or downstream service missing; check `docker compose logs api-gateway`
         - SC#3-route fails → gateway route table missing the `/__health/<svc>` entry, or downstream `/__health` controller missing (per D-01)
         - SC#4 fails → frontend container failed to start; check `docker compose logs frontend`
         - SC#5 fails → Pitfall 3 hit: per-service Flyway history table not configured; check that `application.yml` for the service contains `spring.flyway.table=<service>_flyway_schema_history`, AND that `flyway-database-postgresql` is on the runtime classpath (Pitfall A).
    4. **Manual verifications** that smoke does NOT cover (cite VALIDATION.md "Manual-Only Verifications"):
       - Eureka dashboard renders 4 services as registered (visit `http://localhost:8761/`)
       - Frontend landing page renders without browser console errors (open `http://localhost:5173` + DevTools)
       - 360px viewport responsiveness check (Chrome DevTools mobile sim)
       - Dark-mode CSS variables wire correctly (manually toggle `.dark` class via DevTools)
    5. **Adding more scripts**: Future scripts (e.g., `scripts/seed-cities.sh` for Phase 3) should follow the same conventions: `#!/usr/bin/env bash`, `set -euo pipefail`, labelled failures.
  </action>
  <verify>
    <automated>test -f scripts/README.md && grep -q 'smoke.sh' scripts/README.md && grep -q 'SC#1' scripts/README.md && grep -q 'SC#5' scripts/README.md && grep -q 'COMPOSE_FILE' scripts/README.md && grep -q 'flyway-database-postgresql' scripts/README.md && grep -q 'localhost:8761' scripts/README.md</automated>
  </verify>
  <acceptance_criteria>
    - All 5 SC labels documented with what each asserts and what to do on failure.
    - Manual verifications section cites that frontend console-error check, Eureka dashboard visual, and 360px responsiveness are manual.
    - Pitfall 3 (Flyway history) and Pitfall A (`flyway-database-postgresql` driver) explicitly named in the SC#5 failure-mode pointer.
  </acceptance_criteria>
  <done>Anyone running smoke.sh can read the README and understand both what it asserts and how to debug any failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev terminal ↔ docker compose | Smoke script runs `docker compose exec postgres psql` — uses host-side postgres credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-00-06 | Information Disclosure | smoke.sh logs containing postgres credentials in CI output | LOW | mitigate | Script uses env-var defaults (`POSTGRES_USER`, `POSTGRES_DB`) — not hardcoded; `psql` invocation does not echo password (uses peer auth inside container). |
| T-00-07 | Denial of Service | smoke.sh hangs indefinitely on a flaky service preventing CI completion | LOW | mitigate | `WAIT_TIMEOUT` env var with 60s default; curl uses `-sf` (fail on non-2xx); SC#2 uses bounded retry (30s max). |
| T-00-08 | Tampering | Future contributors edit smoke.sh to relax assertions to make CI pass | LOW | accept | Human review; no automated mitigation in Phase 0. Phase 10 hardening adds CI-side dependency-check that catches scope creep. |
</threat_model>

<verification>
- `bash -n scripts/smoke.sh` exits 0.
- `scripts/smoke.sh` exits non-zero (SC#1 failure expected) when no compose stack is running — proves it does not hang or false-pass.
- `scripts/README.md` exists and references all 5 SC labels.
</verification>

<success_criteria>
- `scripts/smoke.sh` is executable, bash-syntax-valid, and emits all 5 SC labels.
- Script provides a one-shot phase verification — Wave 6 (CI workflows + final smoke) and `/gsd-verify-phase` both rely on it.
- `scripts/README.md` documents usage, criteria, and failure-mode debugging pointers.
</success_criteria>

<output>
After completion, create `.planning/phases/00-monorepo-scaffolding/00-02-SUMMARY.md` summarizing:
- Smoke script line count + criterion-coverage map.
- Whether `jq` fallback is implemented for `grep`-only environments.
- Notes on what the script CANNOT verify (manual checks deferred per VALIDATION.md).
</output>
