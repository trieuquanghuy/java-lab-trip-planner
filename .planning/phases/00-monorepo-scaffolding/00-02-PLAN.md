---
phase: 00-monorepo-scaffolding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/smoke.sh
  - scripts/README.md
autonomous: true
requirements: [NFR-04]

must_haves:
  truths:
    - "`scripts/smoke.sh` is the canonical phase-gate verifier; exits non-zero on any criterion failure with a printed criterion id (D-33)."
    - "Smoke script enforces all 5 ROADMAP success criteria PLUS NFR-04 (free-tier-only audit): compose healthy <60s, 4 services in Eureka, gateway health UP, frontend reachable, per-service Flyway history tables present, no paid-SaaS dependencies in `gradle/libs.versions.toml`/`frontend/package.json`/`infra/docker-compose.yml`."
    - "Smoke script supports per-criterion gating (`--criterion <N>`) so each wave can sanity-check what is in scope at that point — Wave 1 can run `--list`, Wave 2 can build libs, Wave 3 can hit `--criterion 2` (Eureka) once services exist, Wave 4 can hit `--criterion 1` (compose <60s) once compose lands, Wave 5 runs full gate (D-33 'incremental smoke-test as containers come online' enforcement)."
    - "Smoke script lands in Wave 1 (NOT a final wave) so each subsequent wave's containers can be smoke-tested incrementally as they come online (D-33)."
    - "NFR-04 free-tier audit uses an enumerated grep deny-list (NOT a vague 'no paid deps' check) — listed paid-SaaS Java SDKs and npm packages are matched against `gradle/libs.versions.toml`, `frontend/package.json`, and `infra/docker-compose.yml`."
  artifacts:
    - path: "scripts/smoke.sh"
      provides: "Executable phase-gate verifier wrapping `docker compose up -d --wait` and asserting all 5 ROADMAP SCs + NFR-04. Supports `--criterion <N>` for per-wave gating, `--up`/`--down` for stack control, `--list` for criterion enumeration."
      contains: "set -euo pipefail"
      min_lines: 80
    - path: "scripts/README.md"
      provides: "Documentation of how to run smoke.sh, what each criterion means, what failure modes look like, and how to use per-criterion gating during incremental wave execution"
  key_links:
    - from: "scripts/smoke.sh"
      to: "infra/docker-compose.yml (Wave 4)"
      via: "`docker compose ps` / `docker compose exec postgres psql` queries"
      pattern: "docker compose (ps|exec)"
    - from: "scripts/smoke.sh"
      to: "Eureka dashboard at localhost:8761"
      via: "curl GET /eureka/apps with Accept: application/json"
      pattern: "localhost:8761/eureka"
    - from: "scripts/smoke.sh NFR-04 audit"
      to: "gradle/libs.versions.toml + frontend/package.json + infra/docker-compose.yml"
      via: "grep deny-list of paid-SaaS Java/npm packages"
      pattern: "algolia|auth0|mapbox|twilio|sentry"
    - from: ".github/workflows/backend.yml + frontend.yml (Wave 6)"
      to: "scripts/smoke.sh"
      via: "potential CI step (added in Wave 6 plan 13)"
      pattern: "scripts/smoke.sh"
---

<objective>
Land the canonical phase-gate verifier `scripts/smoke.sh` and its README in Wave 1 per D-33. By placing smoke FIRST (not last), every subsequent wave can validate its contribution incrementally — Wave 2 lib changes don't break smoke, Wave 3 service skeletons must pass smoke at health-check level before merge, etc.

This Wave-1 placement is operationalized by per-criterion gating (`--criterion <N>`) so executors can run only the SCs that are reachable at each wave: Wave 1 has nothing to verify (script just needs to exist and `--list` correctly), Wave 2 verifies libs build, Wave 3 unlocks SC#2 (Eureka registration), Wave 4 unlocks SC#1 (compose <60s), Wave 5 unlocks SC#4 (frontend reachability) and full gate.

Smoke also enforces NFR-04 (free-tier-only audit) via an enumerated grep deny-list applied to `gradle/libs.versions.toml`, `frontend/package.json`, and `infra/docker-compose.yml`. This is the BLOCKER 4 fix — NFR-04 is no longer listed in `requirements:` without an actual verifier.

Purpose: D-33 explicitly requires this script in Wave 1 with incremental gating. The script doubles as the verification harness for `/gsd-verify-phase` and provides the executor a one-line "did I break Phase 0?" check after every change.

Output: Executable `scripts/smoke.sh` that exits 0 when all 5 ROADMAP success criteria + NFR-04 pass, supports `--criterion <N>` for per-wave gating, and a `scripts/README.md` documenting how to read its output and how to use it incrementally during execution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/00-monorepo-scaffolding/00-CONTEXT.md
@.planning/phases/00-monorepo-scaffolding/00-RESEARCH.md
@.planning/phases/00-monorepo-scaffolding/00-PATTERNS.md
@.planning/phases/00-monorepo-scaffolding/00-VALIDATION.md
@.planning/phases/00-monorepo-scaffolding/00-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 2.1: Author scripts/smoke.sh — full SC#1-#5 + NFR-04 audit + per-criterion gating (--criterion N) + --up/--down/--list</name>
  <files>scripts/smoke.sh</files>
  <read_first>
    - .planning/phases/00-monorepo-scaffolding/00-CONTEXT.md (lines 175-183 — D-33 — smoke as Wave 1 deliverable; what 5 criteria must be enforced; "incremental smoke-test as containers come online")
    - .planning/phases/00-monorepo-scaffolding/00-PATTERNS.md (lines 901-950 — Bucket I — full smoke script excerpt)
    - .planning/phases/00-monorepo-scaffolding/00-RESEARCH.md (lines 1283-1336 — Validation Architecture; Sampling Rate; smoke script outline; A9 — NFR-04 satisfied by no paid SaaS deps)
    - .planning/ROADMAP.md (lines 30-39 — Phase 0 Success Criteria #1-#5 verbatim — these MUST be the assertions)
    - .planning/REQUIREMENTS.md NFR-04 — free-tier-only external services
  </read_first>
  <action>
    Create `scripts/smoke.sh` with `#!/usr/bin/env bash` shebang. Use `set -euo pipefail` at the top. The script supports a flag-based dispatch model: subcommands as the first positional arg, or `--criterion <N>` for selective verification.

    1. **Helper functions** at top:
       - `fail() { echo "[smoke] FAIL: $1" >&2; exit 1; }`
       - `pass() { echo "[smoke] OK:   $1"; }`
       - `usage()` — prints flag/criterion help.

    2. **Configuration** (env vars with defaults; settable from compose `.env`):
       - `COMPOSE_FILE="${COMPOSE_FILE:-infra/docker-compose.yml}"`
       - `POSTGRES_DB="${POSTGRES_DB:-tripplanner}"`
       - `POSTGRES_USER="${POSTGRES_USER:-postgres}"`
       - `WAIT_TIMEOUT="${WAIT_TIMEOUT:-60}"` (seconds)

    3. **Flag parsing — case dispatch on the first arg.** Order matters; the script's outermost `case` selects mode:
       - `--list` → enumerate available criterion labels and exit 0.
       - `--up` → `docker compose -f "$COMPOSE_FILE" up -d --wait --wait-timeout "$WAIT_TIMEOUT"` and exit 0 (no SC checks).
       - `--down` → `docker compose -f "$COMPOSE_FILE" down -v` and exit 0.
       - `--criterion <N>` → run ONLY criterion N (where N ∈ `{1,2,3,3-route,4,5,nfr-04}`), exit on its result.
       - `--help` / `-h` → print usage and exit 0.
       - (no flag) → run full SC#1-#5 + NFR-04, exit non-zero on any failure (Phase 0 final gate).

    4. **Per-criterion functions** — each is a small bash function that asserts ONE criterion. Implement:

       - **`check_sc1()` — SC#1 compose healthy <60s**: `docker compose -f "$COMPOSE_FILE" ps --format json | jq -e 'all(.[]; .State == "running" and (.Health == "healthy" or .Health == ""))'` (`""` covers services without a healthcheck like mailhog/zipkin). Fail with id `SC#1` if any service is not running or healthy. Note: in `--up` preflight mode, `--wait-timeout` already enforces the 60s budget; this check just verifies steady-state health AFTER up returns.

       - **`check_sc2()` — SC#2 4 services in Eureka**: `curl -sf -H "Accept: application/json" http://localhost:8761/eureka/apps` returns JSON; for each of `AUTH-SERVICE`, `TRIP-SERVICE`, `DESTINATION-SERVICE`, `API-GATEWAY`, grep the response (or use `jq '.applications.application[].name'`) and fail with id `SC#2` listing the missing service if absent. Loop with a max-30s retry+1s sleep window because Eureka registration may complete shortly after compose health.

       - **`check_sc3()` — SC#3 gateway /actuator/health UP**: `curl -sf http://localhost:8080/actuator/health | jq -e '.status == "UP"'`. Fail with id `SC#3`.

       - **`check_sc3_route()` — SC#3-route `/__health/<svc>` end-to-end**: For `svc in auth trip destination`: `curl -sf "http://localhost:8080/__health/$svc" | jq -e '.status == "UP" and .phase == 0'`. Fail with id `SC#3-route` listing the failing service.

       - **`check_sc4()` — SC#4 frontend reachable**: `curl -sf -o /tmp/tripplanner_index.html http://localhost:5173 && test -s /tmp/tripplanner_index.html`. Fail with id `SC#4`. (Browser console-error check is manual per VALIDATION.md "Manual-Only Verifications"; smoke only asserts HTTP reachability + non-empty body.)

       - **`check_sc5()` — SC#5 per-service Flyway history tables**: For each `(schema, table)` pair in `("auth", "auth_flyway_schema_history")`, `("trip", "trip_flyway_schema_history")`, `("destination", "destination_flyway_schema_history")`: `EXISTS=$(docker compose -f "$COMPOSE_FILE" exec -T postgres psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -tAc "SELECT 1 FROM information_schema.tables WHERE table_schema='$schema' AND table_name='$table'")`. Fail with id `SC#5` listing `$schema.$table` if not `1`.

       - **`check_nfr_04()` — NFR-04 free-tier audit (BLOCKER 4 fix)**: enumerated grep deny-list. Define the list explicitly in the script body (NOT a vague "no paid deps" heuristic):
         ```bash
         # Enumerated paid-SaaS deny-list. Add to this list as new paid services are evaluated.
         # Source: NFR-04 (free-tier only); CONTEXT.md "Out of scope: paid SaaS deps".
         JAVA_DENY_LIST="algolia auth0-java mapbox-java twilio sentry-spring sentry-logback datadog-api-client newrelic-java rollbar-java bugsnag-java pingdom"
         NPM_DENY_LIST="@algolia/ @mapbox/ @auth0/ @sentry/ @datadog/ @newrelic/ @bugsnag/ @rollbar/ @pingdom/ twilio"
         COMPOSE_DENY_LIST="datadoghq.com sentry.io algolia.net auth0.com mapbox.com twilio.com newrelic.com bugsnag.com rollbar.com pingdom.com"
         ```
         Implementation:
         - For each token in `JAVA_DENY_LIST`: `if grep -qi "$token" gradle/libs.versions.toml 2>/dev/null; then fail "SC#NFR-04: paid Java SDK '$token' found in gradle/libs.versions.toml"; fi`
         - For each token in `NPM_DENY_LIST`: `if grep -q "$token" frontend/package.json 2>/dev/null; then fail "SC#NFR-04: paid npm package '$token' found in frontend/package.json"; fi`
         - For each token in `COMPOSE_DENY_LIST`: `if grep -q "$token" infra/docker-compose.yml 2>/dev/null; then fail "SC#NFR-04: paid SaaS reference '$token' found in compose file"; fi`
         - On success: `pass "NFR-04 free-tier audit (no paid SaaS deps in catalog/package.json/compose)"`
         - **Important**: each file probe is guarded with `2>/dev/null` and `test -f` so the audit does NOT fail simply because a file does not yet exist (Wave 1 invocation must be safe before Wave 4/5 land their files).

    5. **Full-gate dispatcher** (no flag, or after `--up`):
       ```bash
       check_sc1
       check_sc2
       check_sc3
       check_sc3_route
       check_sc4
       check_sc5
       check_nfr_04
       echo "[smoke] ALL PASS — Phase 0 ROADMAP success criteria #1-#5 + NFR-04 met"
       ```

    6. **`--criterion N` dispatcher** — case match on the criterion label:
       ```bash
       case "$CRITERION" in
         1)        check_sc1 ;;
         2)        check_sc2 ;;
         3)        check_sc3 ;;
         3-route)  check_sc3_route ;;
         4)        check_sc4 ;;
         5)        check_sc5 ;;
         nfr-04)   check_nfr_04 ;;
         *)        fail "Unknown criterion '$CRITERION'. Use --list to see available criteria." ;;
       esac
       ```

    7. **`--list` dispatcher** — print available criteria:
       ```
       Available criteria for `scripts/smoke.sh --criterion <N>`:
         1         — SC#1: compose healthy <60s
         2         — SC#2: 4 services in Eureka
         3         — SC#3: gateway /actuator/health UP
         3-route   — SC#3-route: /__health/<svc> end-to-end via gateway
         4         — SC#4: frontend reachable at :5173
         5         — SC#5: per-service Flyway history tables present
         nfr-04    — NFR-04: free-tier audit (no paid SaaS deps)
       ```

    Make the script executable: `chmod +x scripts/smoke.sh`.

    **Robustness requirements:**
    - The script MUST NOT call `exit 1` from inside a subshell context that swallows the non-zero status. Use the `fail()` helper which writes to stderr and exits.
    - Each criterion check is gated by a label (`SC#1`, `SC#2`, `SC#3`, `SC#3-route`, `SC#4`, `SC#5`, `SC#NFR-04`) so a failure tells the developer exactly which ROADMAP/NFR criterion broke.
    - If `jq` is missing, fall back to `grep` for SC#2 / SC#3 / SC#4 (the PATTERNS.md excerpt shows the grep variants); print a one-line note `[smoke] note: jq not found, using grep fallback`.
    - Allow running stand-alone (`./scripts/smoke.sh`), as a preflight (`./scripts/smoke.sh --up`), per-criterion (`./scripts/smoke.sh --criterion 2`), enumeration (`./scripts/smoke.sh --list`), or teardown (`./scripts/smoke.sh --down`).
    - **Wave 1 sanity**: `./scripts/smoke.sh --list` MUST succeed even when no other Phase 0 file exists (no compose, no libs, no services). This proves D-33 incremental usability — the script itself is usable from the moment Wave 1 ships, before any other wave has landed artifacts.

    **Per-wave usage map (document in scripts/README.md, Task 2.2):**
    | Wave | Reasonable smoke invocation | What it asserts |
    |------|-----------------------------|-----------------|
    | 1 | `scripts/smoke.sh --list` | Script is executable + flag handler works |
    | 2 (libs) | `scripts/smoke.sh --criterion nfr-04` | Free-tier audit runs (catalog file exists by now) |
    | 3 (services) | `scripts/smoke.sh --criterion nfr-04` | Audit still passes after services land |
    | 4 (compose) | `scripts/smoke.sh --up && scripts/smoke.sh --criterion 1 && scripts/smoke.sh --criterion 2` | Compose <60s + Eureka registration once compose lands |
    | 5 (frontend) | `scripts/smoke.sh --criterion 4` | Frontend reachable at :5173 |
    | 6 (final) | `scripts/smoke.sh` (full gate, no flag) | All SC#1-#5 + NFR-04 pass |
  </action>
  <verify>
    <automated>test -x scripts/smoke.sh && head -1 scripts/smoke.sh | grep -q '^#!/usr/bin/env bash' && grep -q 'set -euo pipefail' scripts/smoke.sh && grep -q 'SC#1' scripts/smoke.sh && grep -q 'SC#2' scripts/smoke.sh && grep -q 'SC#3' scripts/smoke.sh && grep -q 'SC#4' scripts/smoke.sh && grep -q 'SC#5' scripts/smoke.sh && grep -q 'SC#NFR-04\|NFR-04' scripts/smoke.sh && grep -q 'localhost:8761/eureka/apps' scripts/smoke.sh && grep -q 'localhost:8080/actuator/health' scripts/smoke.sh && grep -q 'localhost:8080/__health' scripts/smoke.sh && grep -q 'localhost:5173' scripts/smoke.sh && grep -q 'flyway_schema_history' scripts/smoke.sh && grep -q 'AUTH-SERVICE\|auth-service' scripts/smoke.sh && grep -q -- '--criterion' scripts/smoke.sh && grep -q -- '--list' scripts/smoke.sh && grep -q -- '--up' scripts/smoke.sh && grep -q -- '--down' scripts/smoke.sh && grep -q 'check_nfr_04\|check_sc' scripts/smoke.sh && grep -q 'JAVA_DENY_LIST\|algolia' scripts/smoke.sh && grep -q 'NPM_DENY_LIST\|@algolia\|@sentry' scripts/smoke.sh && bash -n scripts/smoke.sh && bash scripts/smoke.sh --list 2>&1 | grep -q 'criterion'</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `scripts/smoke.sh`, mode includes `+x`.
    - Bash syntax valid (`bash -n scripts/smoke.sh` exits 0).
    - All 5 SC labels + NFR-04 present in source: `SC#1`, `SC#2`, `SC#3`, `SC#3-route`, `SC#4`, `SC#5`, `SC#NFR-04`.
    - Hits all 5 endpoints/queries: `localhost:8761/eureka/apps`, `localhost:8080/actuator/health`, `localhost:8080/__health/<svc>`, `localhost:5173`, postgres `flyway_schema_history` table query.
    - Supports `--criterion <N>`, `--up`, `--down`, `--list`, `--help`, and bare invocation (full gate).
    - NFR-04 audit uses enumerated deny-lists for Java + npm + compose (NOT a vague "no paid deps" heuristic).
    - NFR-04 audit is safe to run before all files exist (uses `2>/dev/null` guards so missing files don't error).
    - `bash scripts/smoke.sh --list` succeeds standalone (Wave 1 sanity test — no other files needed).
    - Uses `set -euo pipefail`.
    - Has a `fail()` helper that writes to stderr and exits non-zero.
    - When the stack is NOT running, `scripts/smoke.sh` (no flag) fails fast on SC#1 (does not hang for `WAIT_TIMEOUT` seconds unless `--up` flag is passed).
  </acceptance_criteria>
  <done>Smoke script exists, is executable, has correct bash syntax, supports per-criterion gating per D-33, asserts all 5 ROADMAP success criteria + NFR-04 with labelled failure messages, and is safe to invoke from Wave 1 onward (D-33 incremental verification).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2.2: Document smoke.sh usage in scripts/README.md (per-criterion gating + per-wave usage map + NFR-04 deny-list)</name>
  <files>scripts/README.md</files>
  <read_first>
    - .planning/phases/00-monorepo-scaffolding/00-CONTEXT.md (lines 175-183 — D-33; lines 21-22 — phase boundary "all 6 ROADMAP success criteria")
    - .planning/phases/00-monorepo-scaffolding/00-VALIDATION.md (entire file — Wave 0 reqs, sampling rate, manual verifications)
    - .planning/ROADMAP.md (lines 30-39 — Phase 0 Success Criteria — copy verbatim into the README)
    - scripts/smoke.sh (the file you just authored — read it to ensure README docs match)
  </read_first>
  <action>
    Create `scripts/README.md` with the following sections:

    1. **Title:** "Trip Planner — Scripts"
    2. **Overview**: 2 sentences. "This directory contains operational scripts. The primary one is `smoke.sh` — the Phase 0 phase-gate verifier with per-criterion gating per D-33."
    3. **`smoke.sh` — Phase 0 Verifier**:
       - Purpose paragraph: "Asserts all 5 ROADMAP Phase 0 success criteria + NFR-04 (free-tier audit) pass against a running compose stack. Exits 0 on success; non-zero with a labelled failure message (SC#1..SC#5 / SC#NFR-04) on any failure. Supports per-criterion gating so executors can verify incrementally as each wave lands artifacts."
       - Usage:
         ```
         # Standalone full gate (assumes stack already running)
         ./scripts/smoke.sh

         # Bring up the stack (no SC checks)
         ./scripts/smoke.sh --up

         # Tear down + wipe volumes
         ./scripts/smoke.sh --down

         # List available criterion labels
         ./scripts/smoke.sh --list

         # Run ONE criterion only — useful during incremental wave execution
         ./scripts/smoke.sh --criterion 1          # SC#1 compose <60s
         ./scripts/smoke.sh --criterion 2          # SC#2 Eureka
         ./scripts/smoke.sh --criterion 3          # SC#3 gateway health
         ./scripts/smoke.sh --criterion 3-route    # SC#3-route /__health/<svc>
         ./scripts/smoke.sh --criterion 4          # SC#4 frontend
         ./scripts/smoke.sh --criterion 5          # SC#5 Flyway history tables
         ./scripts/smoke.sh --criterion nfr-04     # NFR-04 free-tier audit
         ```
       - Environment overrides table: `COMPOSE_FILE` (default `infra/docker-compose.yml`), `POSTGRES_DB` (default `tripplanner`), `POSTGRES_USER` (default `postgres`), `WAIT_TIMEOUT` (default `60`).
       - **6 criteria** table — copy verbatim from `.planning/ROADMAP.md` Phase 0 Success Criteria #1-#5 with the SC label the script emits, plus NFR-04:
         | Criterion | What it asserts |
         |-----------|-----------------|
         | SC#1 | `docker compose up` brings every container to healthy status in under 60 seconds with no manual intervention |
         | SC#2 | All 4 services (api-gateway, auth-service, trip-service, destination-service) registered in Eureka |
         | SC#3 | `curl localhost:8080/actuator/health` returns `{"status":"UP"}` through the gateway |
         | SC#3-route | `/__health/{auth,trip,destination}` route through the gateway and return `{status: UP, phase: 0}` (D-02 validation) |
         | SC#4 | `localhost:5173` reachable (frontend dev server up) — note: console-error check is manual |
         | SC#5 | Per-service `*_flyway_schema_history` tables exist in their respective schemas |
         | SC#NFR-04 | `gradle/libs.versions.toml`, `frontend/package.json`, `infra/docker-compose.yml` contain no enumerated paid-SaaS deps (free-tier audit) |
       - **NFR-04 deny-list (enumerated)** — copy from smoke.sh body so reviewers can audit without re-reading the script:
         - Java SDKs: `algolia`, `auth0-java`, `mapbox-java`, `twilio`, `sentry-spring`, `sentry-logback`, `datadog-api-client`, `newrelic-java`, `rollbar-java`, `bugsnag-java`, `pingdom`
         - npm packages: `@algolia/`, `@mapbox/`, `@auth0/`, `@sentry/`, `@datadog/`, `@newrelic/`, `@bugsnag/`, `@rollbar/`, `@pingdom/`, `twilio`
         - Compose service refs: `datadoghq.com`, `sentry.io`, `algolia.net`, `auth0.com`, `mapbox.com`, `twilio.com`, `newrelic.com`, `bugsnag.com`, `rollbar.com`, `pingdom.com`
         - To extend: edit the `JAVA_DENY_LIST` / `NPM_DENY_LIST` / `COMPOSE_DENY_LIST` arrays in `scripts/smoke.sh`. Adds appear in this README at the next `/gsd-update-state`.
       - **Per-wave usage map** — incremental verification matrix:
         | Wave | Recommended smoke invocation | What it asserts at this point |
         |------|------------------------------|-------------------------------|
         | 1 (build infra + smoke + observability + error-handling) | `scripts/smoke.sh --list` | Script is executable + flag handler works |
         | 2 (libs) | `scripts/smoke.sh --criterion nfr-04` | Free-tier audit runs (catalog file exists) |
         | 3 (service skeletons) | `scripts/smoke.sh --criterion nfr-04` | Audit still passes after services land |
         | 4 (compose stack) | `scripts/smoke.sh --up && scripts/smoke.sh --criterion 1 && scripts/smoke.sh --criterion 2` | Compose healthy <60s + Eureka registration |
         | 5 (frontend) | `scripts/smoke.sh --criterion 4` | Frontend reachable at :5173 |
         | 6 (CI + final smoke) | `scripts/smoke.sh` (no flag — full gate) | All SC#1-#5 + NFR-04 pass |
       - **Failure modes** subsection — for each SC, a one-line "if this fails, look at:" pointer:
         - SC#1 fails → check `docker compose ps`, look for non-`healthy` services
         - SC#2 fails → likely Eureka cold-start; wait 30s and re-run; if persistent, check service logs for registration errors
         - SC#3 fails → gateway misconfigured or downstream service missing; check `docker compose logs api-gateway`
         - SC#3-route fails → gateway route table missing the `/__health/<svc>` entry, or downstream `/__health` controller missing (per D-01)
         - SC#4 fails → frontend container failed to start; check `docker compose logs frontend`
         - SC#5 fails → Pitfall 3 hit: per-service Flyway history table not configured; check that `application.yml` for the service contains `spring.flyway.table=<service>_flyway_schema_history`, AND that `flyway-database-postgresql` is on the runtime classpath (Pitfall A).
         - SC#NFR-04 fails → a paid-SaaS dep was added; either (a) remove the dep, (b) replace with a free-tier alternative, or (c) get explicit dispensation from the developer + update REQUIREMENTS.md.
    4. **Manual verifications** that smoke does NOT cover (cite VALIDATION.md "Manual-Only Verifications"):
       - Eureka dashboard renders 4 services as registered (visit `http://localhost:8761/`)
       - Frontend landing page renders without browser console errors (open `http://localhost:5173` + DevTools)
       - 360px viewport responsiveness check (Chrome DevTools mobile sim)
       - Dark-mode CSS variables wire correctly (manually toggle `.dark` class via DevTools)
    5. **Adding more scripts**: Future scripts (e.g., `scripts/seed-cities.sh` for Phase 3) should follow the same conventions: `#!/usr/bin/env bash`, `set -euo pipefail`, labelled failures, `--help` flag.
  </action>
  <verify>
    <automated>test -f scripts/README.md && grep -q 'smoke.sh' scripts/README.md && grep -q 'SC#1' scripts/README.md && grep -q 'SC#5' scripts/README.md && grep -q 'NFR-04' scripts/README.md && grep -q 'COMPOSE_FILE' scripts/README.md && grep -q 'flyway-database-postgresql' scripts/README.md && grep -q 'localhost:8761' scripts/README.md && grep -q -- '--criterion' scripts/README.md && grep -q -- '--list' scripts/README.md && grep -q -i 'per-wave' scripts/README.md && grep -q 'algolia\|sentry\|auth0' scripts/README.md && grep -q -i 'deny-list\|deny list' scripts/README.md</automated>
  </verify>
  <acceptance_criteria>
    - All 5 SC labels + NFR-04 documented with what each asserts and what to do on failure.
    - Per-criterion gating documented with copy-pasteable invocations.
    - Per-wave usage map present (6 rows: Wave 1 through Wave 6 with recommended invocation).
    - NFR-04 deny-list enumerated explicitly in the README so reviewers can audit without re-reading the script.
    - Manual verifications section cites that frontend console-error check, Eureka dashboard visual, and 360px responsiveness are manual.
    - Pitfall 3 (Flyway history) and Pitfall A (`flyway-database-postgresql` driver) explicitly named in the SC#5 failure-mode pointer.
  </acceptance_criteria>
  <done>Anyone running smoke.sh can read the README, understand the per-criterion gating model, look up which criterion corresponds to which wave, audit the NFR-04 deny-list, and debug any failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev terminal ↔ docker compose | Smoke script runs `docker compose exec postgres psql` — uses host-side postgres credentials |
| smoke script ↔ committed source files | NFR-04 audit greps committed source for paid-SaaS deps; if the deny-list is bypassed by mis-spelling, audit returns false-positive pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-00-06 | Information Disclosure | smoke.sh logs containing postgres credentials in CI output | LOW | mitigate | Script uses env-var defaults (`POSTGRES_USER`, `POSTGRES_DB`) — not hardcoded; `psql` invocation does not echo password (uses peer auth inside container). |
| T-00-07 | Denial of Service | smoke.sh hangs indefinitely on a flaky service preventing CI completion | LOW | mitigate | `WAIT_TIMEOUT` env var with 60s default; curl uses `-sf` (fail on non-2xx); SC#2 uses bounded retry (30s max). |
| T-00-08 | Tampering | Future contributors edit smoke.sh to relax assertions to make CI pass | LOW | accept | Human review; no automated mitigation in Phase 0. Phase 10 hardening adds CI-side dependency-check that catches scope creep. |
| T-00-09 | Tampering / Bypass | Developer adds a paid-SaaS dep with a name not in the deny-list (e.g., `@some-new-paid-vendor/sdk`) — SC#NFR-04 returns false-pass | MEDIUM | accept (process control) | Deny-list is a positive-affirmation tool, not a complete defense. Code review catches new deps; the deny-list is updated as new paid services are evaluated. README documents the extension procedure. |
</threat_model>

<verification>
- `bash -n scripts/smoke.sh` exits 0.
- `scripts/smoke.sh --list` exits 0 with criterion labels printed (Wave 1 sanity — no other files needed).
- `scripts/smoke.sh` (no flag) exits non-zero (SC#1 failure expected) when no compose stack is running — proves it does not hang or false-pass.
- `scripts/smoke.sh --criterion nfr-04` exits 0 in Wave 2+ once `gradle/libs.versions.toml` exists with no paid-SaaS deps; exits non-zero with `SC#NFR-04` label if a deny-listed token is found.
- `scripts/README.md` exists and references all 5 SC labels + NFR-04 deny-list.
</verification>

<success_criteria>
- `scripts/smoke.sh` is executable, bash-syntax-valid, and emits all 5 SC labels + NFR-04.
- Script supports `--criterion <N>` for per-wave gating (D-33 incremental verification).
- Script provides a one-shot full-phase verification — Wave 6 (CI workflows + final smoke) and `/gsd-verify-phase` both rely on it.
- NFR-04 audit uses an enumerated grep deny-list (BLOCKER 4 fix — concrete verification of `requirements: [NFR-04]`).
- `scripts/README.md` documents usage, per-criterion gating, per-wave usage map, NFR-04 deny-list, and failure-mode debugging pointers.
</success_criteria>

<output>
After completion, create `.planning/phases/00-monorepo-scaffolding/00-02-SUMMARY.md` summarizing:
- Smoke script line count + criterion-coverage map (must include 7 criterion functions: sc1, sc2, sc3, sc3-route, sc4, sc5, nfr-04).
- Whether `jq` fallback is implemented for `grep`-only environments.
- Per-criterion gating verified (`scripts/smoke.sh --list` runs standalone).
- NFR-04 deny-list: count of tokens (Java + npm + compose).
- Notes on what the script CANNOT verify (manual checks deferred per VALIDATION.md).
</output>
